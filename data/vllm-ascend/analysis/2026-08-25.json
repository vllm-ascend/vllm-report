{
  "date": "2026-08-25",
  "repo": "vllm-project/vllm-ascend",
  "generated_at": "2026-08-26T02:55:25.612596+08:00",
  "daily_summary": "当日变更以测试配置清理、CI 调整和文档更新为主，同时包含多项代码修复与一个大型底层算子新增。重点包括：清理 weekly/nightly 中过期 additional_config 键以适配严格配置校验；新增 Qwen3.5 纯文本 checkpoint 的 RoPE 与 ModelSlim 映射支持；优化 FIA 图重放路径避免配置查找开销；新增 DSA + CPU offloading 场景的 FusedSparseAttentionOverlap 融合算子；修复 310P MoE ACL Graph 捕获、MiniMax M3 MXFP8 RMSNorm 与稀疏索引边界、x86 Triton stub 等问题；移除废弃的 causal_conv1d_update_npu；新增 RL 使用文档和多项 CI/测试配置。",
  "test_impact_summary": "当日测试看护影响较大：大量 weekly/nightly YAML 配置迁移和特征提取逻辑变更需要 CI 验证配置可解析性；新增的 FusedSparseAttentionOverlap 算子需要精度、性能和集成测试，且 950 端到端集成尚未启用；Qwen3.5 纯文本支持、MiniMax M3 修复和 310P MoE 修复需要真实 NPU 回归验证；Triton stub 修复缺少针对 x86/aarch64 两种 triton 布局的单元测试；causal_conv1d_update_npu 删除后需确认 Mamba 模型仍走正确的 AscendC/310P 路径。现有测试大多已随 commit 更新，但真实 NPU 行为仍依赖 CI 执行。",
  "commits": [
    {
      "sha": "6fd6ea161b904ee4b379f6a18bc29d3e076cf25a",
      "comment": "清理 weekly/nightly 测试配置中的过期 additional_config 键，以适配 AscendConfig(extra=forbid) 严格校验。将编译选项移入 ascend_compilation_config，lmhead_tensor_parallel_size 移入 finegrained_tp_config，enable_weight_nz_layout 替换为 weight_nz_mode:1，EPLB 旧键迁移到 eplb_config 嵌套结构，移除已删除的 Ascend Scheduler 配置并改用 --enable-chunked-prefill，同时同步更新特征提取逻辑。风险低，但涉及大量 YAML，需确保所有配置可被当前 schema 解析。",
      "tags": [
        "chore",
        "low-risk",
        "config",
        "test"
      ],
      "test_impact": {
        "needs_test_update": true,
        "reason": "大量 YAML 配置迁移和特征提取逻辑变更，需要 CI 验证所有配置可解析且特征提取正确。",
        "suggested_test_areas": [
          "tests/e2e/weekly",
          "tests/e2e/nightly",
          "tests/e2e/nightly/single_node/models/scripts/test_single_node.py"
        ]
      },
      "message": "[BugFix][Config] Clean up stale weekly and nightly additional configs (#14934)\n\n### What this PR does / why we need it?\n\nThe strict `AscendConfig(extra=forbid)` validation introduced by #13838\nexposes stale keys in weekly and nightly smoke-test YAML files.\n\nThis PR:\n\n- moves compilation options such as `enable_npugraph_ex`,\n`enable_static_kernel`, and `fuse_muls_add` under\n`ascend_compilation_config`;\n- moves `lmhead_tensor_parallel_size` under `finegrained_tp_config`;\n- replaces the removed `enable_weight_nz_layout` switch with\n`weight_nz_mode: 1`;\n- migrates legacy EPLB names into the current nested `eplb_config`\nschema, following #5533;\n- replaces the removed Ascend Scheduler wrapper with the vLLM\n`--enable-chunked-prefill` option, following #4623;\n- removes disabled or obsolete configuration entries with no current\nruntime consumer;\n- updates single-node feature extraction to detect NZ layout through\n`weight_nz_mode != 0`.\n\n### Does this PR introduce _any_ user-facing change?\n\nNo. This only updates test configurations and their feature-reporting\nmetadata to match the current validated configuration schema.\n\n### How was this patch tested?\n\n- `git diff --check`\n- Parsed all 157 YAML files under `tests/e2e/weekly` and\n`tests/e2e/nightly` and all 309 remaining embedded `--additional-config`\nJSON values.\n- Checked top-level and nested keys against the current `AscendConfig`\nschemas with no unknown keys found.\n- Verified that the migrated legacy keys no longer occur in\nweekly/nightly YAML files.\n- `codespell`, `typos`, and `tools/check_symbolic_meta.py` passed\nlocally.\n- Constructed Pydantic `AscendConfig` instances for the original 13\nsingle-node migrations and verified `weight_nz_mode` feature extraction\nfor values 0, 1, and 2.\n\nFull NPU smoke testing is left to CI because the local WSL CPU\nenvironment does not provide `torch_npu`.\n\n- vLLM version: v0.27.1\n- vLLM main:\nhttps://github.com/vllm-project/vllm/commit/ba07e4a48fc951300d97eb506217dd530583dea3\n\n---------\n\nSigned-off-by: RuiZhang <wfzrpku@gmail.com>"
    },
    {
      "sha": "c8618d604aa4487ddfe1eac13339d921a33f6e9b",
      "comment": "更新 Qwen3.8-27B 模型文档，新增 Ascend950PR 系列支持说明、部署示例和 benchmark 参数表。纯文档变更，无生产代码影响。",
      "tags": [
        "docs",
        "low-risk"
      ],
      "test_impact": {
        "needs_test_update": false,
        "reason": "文档变更，无测试影响。",
        "suggested_test_areas": []
      },
      "message": "[Doc] update Qwen3.8-27B model documentation add 950 pr and benchmark (#14673)\n\nupdate Qwen3.8-27B model documentation add 950 pr and benchmark\n\n- vLLM version: v0.27.1\n- vLLM main:\nhttps://github.com/vllm-project/vllm/commit/ba07e4a48fc951300d97eb506217dd530583dea3\n\nSigned-off-by: yiminghub2024 <482890@qq.com>"
    },
    {
      "sha": "5d096189a5b710f83d22177982d2236dd0c951d0",
      "comment": "为 Qwen3.5 纯文本 checkpoint 提供 RoPE 路径选择和 ModelSlim packed-module 映射。通过 _uses_multimodal_rope 检查 rotary_emb 是否具有 mrope_section，避免纯文本模型错误进入多模态 RoPE 路径；新增 qwen3_5_text/qwen3_5_moe_text 的 packed 映射。范围受限，未包含模型注册等上游改动。风险中等，涉及注意力 patch 和量化映射，但已有单测覆盖。",
      "tags": [
        "feature",
        "medium-risk",
        "patch",
        "quantization"
      ],
      "test_impact": {
        "needs_test_update": true,
        "reason": "已有单测覆盖 RoPE 选择和 ModelSlim 映射，但缺少真实 NPU 推理验证，建议补充 Qwen3.5 纯文本 checkpoint 的 e2e 冒烟测试。",
        "suggested_test_areas": [
          "tests/ut/patch/worker/test_patch_qwen3_5_mtp.py",
          "tests/ut/quantization/test_modelslim_config.py",
          "tests/e2e/pull_request/one_card"
        ]
      },
      "message": "[Feature][Model] Support text-only Qwen3.5 checkpoints (#14885)\n\n### What this PR does / why we need it?\nThis PR backports the Qwen3.5 text-only RoPE handling and ModelSlim\npacked-module mappings from [vLLM PR\n#50734](https://github.com/vllm-project/vllm/pull/50734).\nText-only Qwen3.5-family checkpoints use the model types qwen3_5_text or\nqwen3_5_moe_text. Unlike multimodal Qwen3.5 checkpoints, they do not\nexpose multimodal RoPE fields such as mrope_section.\nBefore this change, the Ascend Qwen3.5 attention patch selected the\nmultimodal RoPE path based only on the qwen3_5 model-type prefix. This\ncould cause text-only checkpoints to access missing multimodal RoPE\nattributes. ModelSlim also lacked packed-module mappings for the\ntext-only dense and MoE model types.\nThe change:\n- selects the multimodal RoPE path only when mrope_section is available;\n- keeps text-only Qwen3.5 checkpoints on the standard RoPE path;\n- adds ModelSlim packed-module mappings for qwen3_5_text;\n- adds ModelSlim packed-module and expert mappings for qwen3_5_moe_text;\n- adds focused unit coverage for RoPE selection and ModelSlim mappings.\nThis is a scoped backport. It does not include the model registration,\nhybrid-cache wrappers, MTP configuration conversion, or documentation\nchanges from the source PR.\nRelated PR:\n[vllm-project/vllm#50734](https://github.com/vllm-project/vllm/pull/50734)\n\n### Does this PR introduce _any_ user-facing change?\nYes.\nWhen the corresponding text-only Qwen3.5 model integration is available,\ncheckpoints using qwen3_5_text or qwen3_5_moe_text no longer incorrectly\nenter the multimodal RoPE path.\nModelSlim W8A8 checkpoints using these model types can also resolve the\nexisting packed QKV, gate/up projection, GDN projection, and MoE expert\nmappings.\n\n### How was this patch tested?\nAdded unit coverage for:\n- standard RoPE selection for qwen3_5_moe_text;\n- multimodal RoPE selection when mrope_section exists;\n- ModelSlim packed-module mappings for qwen3_5_text;\n- ModelSlim expert mappings for qwen3_5_moe_text.\n\ngit diff --check passed.\n\nPython syntax compilation passed for all four changed files.\n\nTargeted pytest execution was not available in the local environment\nbecause pytest is not installed.\n\nRuff was not available in the local environment, so no Ruff result is\nclaimed.\n\nNo real Ascend NPU or checkpoint inference validation was performed for\nthis scoped backport.\n\nvLLM version: current branch dependency\n\nvLLM main: current branch dependency\n\n- vLLM version: v0.27.1\n- vLLM main:\nhttps://github.com/vllm-project/vllm/commit/ba07e4a48fc951300d97eb506217dd530583dea3\n\nSigned-off-by: Erkong <Erkong@noreply.gitcode.com>\nCo-authored-by: Erkong <Erkong@noreply.gitcode.com>"
    },
    {
      "sha": "afdcd487c76c6fc61f9e7dede76d89c9ef78db6e",
      "comment": "修复 FIA full-graph replay 路径中每次重放都通过 hasattr 读取 hf_text_config.sliding_window 的开销。将 sliding_window 在捕获时存入 attn_params，重放时直接使用捕获值，同时保持 SWA block table 处理。测试同步更新参数列表长度。风险低，但需保证捕获参数顺序与重放一致。",
      "tags": [
        "bugfix",
        "performance",
        "low-risk",
        "attention"
      ],
      "test_impact": {
        "needs_test_update": true,
        "reason": "参数元组新增 sliding_window 字段，需确保所有调用点顺序一致；现有单测已更新，建议补充 SWA 与非 SWA 的图重放对比测试。",
        "suggested_test_areas": [
          "tests/ut/attention/a2/test_attention_v1.py",
          "tests/e2e/pull_request/one_card"
        ]
      },
      "message": "[BugFix] Avoid config lookup in FIA graph replay (#11955)\n\n### What\n- Store sliding_window in full_graph_fia captured attention params.\n- Use the captured value during graph replay instead of reading\nhf_text_config with hasattr.\n\n### Why\nAvoids config lookup overhead in the FIA full-graph replay path while\npreserving the SWA block table handling.\n\n### Tests\n- Fix Qwen3 30B single batch performance reduces 0.5ms\n- Step3.5 AIME accuracy 96.67\n\n\n- vLLM version: v0.27.1\n- vLLM main:\nhttps://github.com/vllm-project/vllm/commit/ba07e4a48fc951300d97eb506217dd530583dea3\n\n---------\n\nSigned-off-by: CXY-Katrina <katrina.cxy@gmail.com>"
    },
    {
      "sha": "c7687f92f1fc6691f9cd0aa51938ff84335a4647",
      "comment": "修正 kv_pool.md 文档中 800 I/T A3 series 硬件描述下的锚点链接。纯文档变更，无测试影响。",
      "tags": [
        "docs",
        "low-risk"
      ],
      "test_impact": {
        "needs_test_update": false,
        "reason": "文档链接修正，无测试影响。",
        "suggested_test_areas": []
      },
      "message": "[Doc][Misc] Fix anchor link in kv_pool documentation (#14882)\n\n### What this PR does / why we need it?\nThis PR corrects a broken anchor link in the `kv_pool.md` documentation\nunder the \"800 I/T A3 series\" hardware description, changing the\nreference to\n`#5322-fabric-memory-size-alignment-a3--ascend_enable_use_fabric_mem1`.\n\n### Does this PR introduce _any_ user-facing change?\nNo, this is a documentation-only update.\n\n### How was this patch tested?\nNo testing is required for this documentation-only change.\n\nSigned-off-by: sunshine202600 <sunshine202600@163.com>"
    },
    {
      "sha": "d1523852b416be34cfea126a9134bb3519771834",
      "comment": "新增 FusedSparseAttentionOverlap 自定义算子，用于 DSA + CPU offloading 场景下将 Selection KV Cache 的 H2D 更新与 Sparse Flash Attention 融合，通过流水调度隐藏未命中 token 的 H2D 延迟。包含 op_host 定义/tiling、arch22 与 arch35 两套 kernel、torch adapter 和精度测试。这是大型底层算子新增，风险高，需重点验证 910C/950 上的数值精度、图模式和内存边界。",
      "tags": [
        "feature",
        "high-risk",
        "ops",
        "attention"
      ],
      "test_impact": {
        "needs_test_update": true,
        "reason": "新增算子需要精度、性能和集成测试；已添加 e2e 精度测试，但 950 端到端集成尚未启用，建议补充更多 shape/dtype 覆盖和边界测试。",
        "suggested_test_areas": [
          "tests/e2e/nightly/single_node/ops/singlecard_ops/test_fused_sparse_attention_overlap.py",
          "tests/ut/attention",
          "csrc/attention/fused_sparse_attention_overlap/tests"
        ]
      },
      "message": "[Feature] Add fused_sparse_attention_overlap operator for DSA with CPU offloading (#14278)\n\n### What this PR does / why we need it?\n\nThis PR implement the Sparse KV Cache Offloading for decode part\nproposed in https://github.com/vllm-project/vllm/issues/48203.\n\nPlease refer to our RFC for more detail about the motivation and\nproposed changes of this PR. The code on the framework side will be\nsubmitted in a subsequent pull request.\n\nThis PR adds a new fused AscendC ttentionOverlap`\nunder`csrc/attention/fused_sparse_attention_overlap/`, targeting\nDeepSeek Sparse Attention (DSA) scenarios where the KV cache is pmory\n(CPU offloading).\n\nIn DSA, only the top-k selected tn. With CPU offloading, selectedtokens\nmay hit the on-device KV cache or miss it and reside in host memory.\nThis operator fuses the sparse attention computation with the\nselection-path KV management (block status / membership map), so that\nthe attention compulaps with and hides the **H2Dloading latency of\nmissed tokens**, instead of stalling the pipeline while waiting for the\nKV transfer to complete.\n\nMain changes:\n\n- **op_host**: op definition (aclistered for `ascend910B`, `ascend910C`\nand `ascend950`), infer-shape/proto, and full tiling implementation\nsupporting `BSND` / `TND` / `PA_BSND` (pageddecode mode.\n- **op_kernel (arch22)**: For 910C device, kernel implementation for\nAscend 910 series, split into cube service, vector service and MLA\nkernel headers.\n- **op_kernel (arch35)**: For 950 device, kernel based on the regbase\nprogrammingmodel (`util_regbase.h`), with dedicated KV-cache handling\n(`fused_sparse_attention_overlap_kvcache.h`).\n- **torch adapter**: `fused_sparst.h` exposing the op to the Pythonside\nwith input checks and output construction (`attention_out`,\n`selection_kv_actual_seq`).\n\n### Does this PR introduce _any_ user-facing change?\n\nYes. A new custom op `FusedSparseAttentionOverlap` is registered and\nexposed to the Python side via the torch adapter. No existing APIs or\nbehaviors are changed.\n### How was this patch tested?\n\n- Added\n`tests/ut/attention/test_fused_sparse_attention_overlap_precision.py`,\nwhich compares the op output against a pure-PyTorch (fp32) reference\nimplementation of paged sparse attention with `rtol=atol=1e-2`, covering\nboth Fn Ascend 950.\n- End-to-end performance with DSA + CPU offloading was verified on\nAscend 910C, showing that the\nH2D loading of missed tokens is hmputation of hit tokens.\n- End-to-end integration on Ascend 950 is not enabled yet and will be\nfollowed up in a subsequent PR.\n\n### Co-Authors\n@Misak2333 \n\n- vLLM version: v0.27.1\n- vLLM main:\nhttps://github.com/vllm-project/vllm/commit/ba07e4a48fc951300d97eb506217dd530583dea3\n\n---------\n\nSigned-off-by: lolo-pop <lolopop.rong@gmail.com>\nSigned-off-by: Misak2333 <167268798+Misak2333@users.noreply.github.com>\nCo-authored-by: Misak2333 <167268798+Misak2333@users.noreply.github.com>"
    },
    {
      "sha": "04da1ce4f61f1726ad0365e88ed8f7249ff19856",
      "comment": "为 triton_q_rms / triton_rms_kernel 增加单卡 e2e 测试，覆盖 fp16/bf16/fp32 和多种 shape，并验证 hidden dim >2048 时抛出 NotImplementedError。纯测试新增，无生产代码变更。",
      "tags": [
        "test",
        "low-risk",
        "ops"
      ],
      "test_impact": {
        "needs_test_update": false,
        "reason": "纯测试新增，已覆盖主要 dtype/shape 和异常路径，无需额外更新。",
        "suggested_test_areas": [
          "tests/e2e/nightly/single_node/ops/singlecard_ops/triton/test_rms_norm.py"
        ]
      },
      "message": "[Test][Triton] Add RMS norm kernel e2e tests (#14700)\n\n### What this PR does / why we need it?\n\nAdds single-card Triton operator test coverage for `triton_q_rms` /\n`triton_rms_kernel`, following the existing tests under\n`tests/e2e/nightly/single_node/ops/singlecard_ops/triton`.\n\nThe new test verifies `triton_q_rms` against an FP32 PyTorch RMSNorm\nbaseline across representative dtype and shape cases, including the\nmaximum supported hidden size. It also checks that hidden dimensions\ngreater than 2048 are rejected.\n\n`triton_q_rms` is used by the DSA Q RMS norm paths through\n`DeviceOperator.apply_dsa_q_rms`, and is also warmed up by\n`rms_triton_warmup`.\n\n### Does this PR introduce _any_ user-facing change?\n\nNo.\n\n### How was this patch tested?\n\n- Added e2e test:\n`tests/e2e/nightly/single_node/ops/singlecard_ops/triton/test_rms_norm.py`\n- Ran syntax check locally:\n`python -m py_compile\ntests/e2e/nightly/single_node/ops/singlecard_ops/triton/test_rms_norm.py`\n- Ran format/lint checks locally:\n`uvx ruff format --check\ntests/e2e/nightly/single_node/ops/singlecard_ops/triton/test_rms_norm.py`\n`uvx ruff check\ntests/e2e/nightly/single_node/ops/singlecard_ops/triton/test_rms_norm.py`\n\nCould not run the NPU e2e test locally on this Windows PC.\n\n- vLLM version: v0.27.1\n- vLLM main:\nhttps://github.com/vllm-project/vllm/commit/ba07e4a48fc951300d97eb506217dd530583dea3\n\nSigned-off-by: GDzhu01 <116337067+GDzhu01@users.noreply.github.com>"
    },
    {
      "sha": "5efd9c05019aa62b910c16f7ef34d37d7638d940",
      "comment": "修复 310P 上 MoE ACL Graph 捕获时 router 权重前向 Cast 不被支持的问题。在 AscendMoERunner310 初始化时检测 internal router 并设置 precast_fp32_weight=True，使权重在加载阶段预转 FP32。测试补充 runner.gate=None 场景。风险低，但需确认 310P 上所有 internal router 路径都会触发预转换。",
      "tags": [
        "bugfix",
        "low-risk",
        "_310p",
        "fused_moe"
      ],
      "test_impact": {
        "needs_test_update": true,
        "reason": "新增 gate 预转换逻辑，现有单测仅覆盖 gate=None 场景，建议补充 gate 存在且无 weight_fp32 属性的用例。",
        "suggested_test_areas": [
          "tests/ut/_310p/fused_moe/test_shared_fused_moe_310.py"
        ]
      },
      "message": "[BugFix][310P] Fix Moe aclgraph error caued by routing to aclop on 310p (#14863)\n\n### What this PR does / why we need it?\nACLGraph Compatibility: Implemented a pre-casting mechanism for router\nweights to prevent unsupported forward-time Cast operations on 310P\nhardware.\n### Does this PR introduce _any_ user-facing change?\nNo\n### How was this patch tested?\n\n\n- vLLM version: v0.27.1\n- vLLM main:\nhttps://github.com/vllm-project/vllm/commit/ba07e4a48fc951300d97eb506217dd530583dea3\n\n---------\n\nSigned-off-by: YangShuai52 <shuaiyang047@163.com>"
    },
    {
      "sha": "f0a93895633b736516dd4e3c664801acca5e2075",
      "comment": "移除已废弃的 Ascend Triton causal_conv1d_update_npu 实现及其 monkey-patch，保留 extract_last_width 和 AscendC/310P 路径。删除两个 skip 测试。风险中等，因为上游 causal_conv1d_update 将恢复默认实现，需确认 Mamba 模型在 Ascend 上仍走正确的 310P/AscendC 路径。",
      "tags": [
        "refactor",
        "medium-risk",
        "ops",
        "patch"
      ],
      "test_impact": {
        "needs_test_update": true,
        "reason": "删除算子后需确认无残留引用，并验证上游 causal_conv1d_update 在 Ascend 上的行为；建议补充 Mamba 模型 e2e 冒烟测试。",
        "suggested_test_areas": [
          "tests/e2e/nightly/single_node/ops/singlecard_ops/triton/test_causal_conv1d.py",
          "tests/e2e/pull_request/one_card"
        ]
      },
      "message": "[Refactor][Ops] Remove causal_conv1d_update_npu (#14620)\n\n### What this PR does / why we need it?\n\nThis PR removes the obsolete Ascend Triton causal_conv1d_update_npu\nimplementation.\n\n- Delete the Triton kernel and Python wrapper.\n- Stop monkey-patching the vLLM causal_conv1d_update function, leaving\nthe upstream implementation in place.\n- Remove the two skipped tests that only exercised the deleted operator.\n- Keep the shared extract_last_width helper and the existing AscendC and\n310P causal-conv paths unchanged.\n\nThe removed tests documented an overflow case and a probabilistic\nfailure, so they did not provide active coverage for this\nimplementation.\n\n### Does this PR introduce _any_ user-facing change?\n\nNo public API or configuration change. Internally, vLLM-Ascend no longer\nreplaces the vLLM causal_conv1d_update function with this Triton\nimplementation.\n\n### How was this patch tested?\n\n- Full repository checks: bash format.sh ci\n- Python syntax compilation for the three changed files\n- Verified there are no remaining references to causal_conv1d_update_npu\nor its Triton kernel\n\nNPU operator tests were not run locally because the operator and its\nskipped tests are removed by this change.\n\n- vLLM version: v0.27.1\n- vLLM main:\nhttps://github.com/vllm-project/vllm/commit/58d3918e3ea0a544ffedadad2ba84559e9c51d8f\n\nSigned-off-by: maoxx241 <maomaoyu870@gmail.com>"
    },
    {
      "sha": "a87e4fc50c3bb3bbd3423b46a72aa60402ef64b4",
      "comment": "更新 upstream workflow 测试用例列表：移除不再通过的 fp8_per_channel 测试，加入 auto_round 测试。CI 配置调整，无生产代码变更。",
      "tags": [
        "ci",
        "low-risk",
        "test"
      ],
      "test_impact": {
        "needs_test_update": false,
        "reason": "仅调整 CI 用例列表，测试选择由 CI 验证。",
        "suggested_test_areas": [
          ".github/workflows/scripts/upstream_config.yaml"
        ]
      },
      "message": "[Test]Update upstream workflow cases (#14888)\n\n### What this PR does / why we need it?\nThis PR add a passed case and remove a case which no longer passes\n### Does this PR introduce _any_ user-facing change?\nNo\n### How was this patch tested?\nby running the test\n- vLLM version: v0.27.1\n- vLLM main:\nhttps://github.com/vllm-project/vllm/commit/ba07e4a48fc951300d97eb506217dd530583dea3\n\n---------\n\nSigned-off-by: jiangyunfan1 <jiangyunfan1@h-partners.com>"
    },
    {
      "sha": "fb5533a6869f8b02e61811933569661fc055850c",
      "comment": "修复 MiniMax M3 MXFP8 的 RMSNorm 在 quant_config 缺少 quant_description 时崩溃的问题，并修复 A5 零前缀 index-score 可能产生负 block-table 访问的问题。RMSNorm 改为 getattr 安全读取，Triton kernel 对 middle 做 max(0, ...) 钳位。测试覆盖新增。风险中等，涉及量化模型数值路径。",
      "tags": [
        "bugfix",
        "medium-risk",
        "models",
        "quantization"
      ],
      "test_impact": {
        "needs_test_update": true,
        "reason": "新增 RMSNorm 和稀疏索引边界测试，但缺少真实 MXFP8 checkpoint 的 e2e 验证，建议补充 MiniMax M3 精度回归。",
        "suggested_test_areas": [
          "tests/ut/ops/test_layernorm.py",
          "tests/e2e/pull_request/one_card/test_minimax_m3_sparse_attn.py"
        ]
      },
      "message": "[BugFix][MiniMax M3] Fix MXFP8 RMSNorm and sparse index bounds (#14704)\n\n### What this PR does / why we need it?\n\nThis PR carries the remaining MiniMax M3 MXFP8 follow-up fixes after\n#14412:\n\n- tolerate quantization configs without `quant_description` in RMSNorm;\n- clamp the A5 zero-prefix index-score range to avoid negative\nblock-table access;\n- add focused unit and E2E regression coverage.\n\nThe previous `SequenceRowParallelOp` / `matmul_and_reduce*` changes have\nbeen removed from this PR. FlashComm v1 and those paths were removed\nfrom `main` by #13946. The upstream SP path communicates after the\nquantization method returns a tensor, so the tuple-reduction\ncompatibility code is no longer applicable.\n\nThis PR intentionally excludes `sparse_attention_score_950`, which is\nhandled separately by #14642.\n\n### Does this PR introduce _any_ user-facing change?\n\nYes. MiniMax M3 MXFP8 configurations without `quant_description` no\nlonger fail during RMSNorm construction, and short/zero-prefix A5\nindex-score cases avoid negative block-table access. There is no new\nAPI, CLI option, or configuration format.\n\n### How was this patch tested?\n\nThe following checks passed locally:\n\n```bash\ngit diff --check upstream/main...HEAD\ngit diff --name-only -z --diff-filter=ACMR upstream/main...HEAD -- '*.py' | xargs -0 python3 -m compileall -q\n```\n\nNPU unit/E2E tests were not run locally because `torch`, `torch_npu`,\n`vllm`, and `pytest` are unavailable in this environment.\n\n- vLLM version: v0.27.1\n- vLLM main:\nhttps://github.com/vllm-project/vllm/commit/cdc4824a21eaa986d4d1fee90a7e6465c9f706e6\n\nSigned-off-by: zonghaoxin <z00946994@china.huawei.com>\nCo-authored-by: zonghaoxin <z00946994@china.huawei.com>"
    },
    {
      "sha": "740e7b9f821230fb1ead96c2ab805cb6018928a9",
      "comment": "新增 vLLM-Ascend for RL 文档，涵盖 RL 模式配置、sleep mode、权重传输、暂停/恢复、logprobs、Router Replay、batch invariance、tokens-only 接口等。纯文档变更，无生产代码影响。",
      "tags": [
        "docs",
        "low-risk"
      ],
      "test_impact": {
        "needs_test_update": false,
        "reason": "文档变更，无测试影响。",
        "suggested_test_areas": []
      },
      "message": "[Doc] Introduces a new documentation page detailing how to utilize vLLM-Ascend for reinforcement learning (RL) workflows (#13712)\n\n### What this PR does / why we need it?\nThis pull request introduces a new documentation page detailing how to\nutilize vLLM-Ascend for reinforcement learning (RL) workflows. It covers\nessential deployment modes, engine lifecycle management, and specific\nfeatures required for RL training, such as sleep mode, weight transfer,\nand batch invariance, ensuring users have a clear reference for setting\nup efficient RL pipelines.\n\nHighlights\nDocumentation Addition: Added a comprehensive guide for using vLLM with\nReinforcement Learning (RL) workloads on Ascend NPUs.\nFeature Integration: Updated navigation and documentation structure to\ninclude the new 'vLLM for RL' feature guide.\n\n### Does this PR introduce _any_ user-facing change?\nNo\n### How was this patch tested?\n\n- vLLM version: v0.27.1\n- vLLM main:\nhttps://github.com/vllm-project/vllm/commit/ba07e4a48fc951300d97eb506217dd530583dea3\n\n---------\n\nSigned-off-by: Ronald1995 <ronaldautomobile@163.com>"
    },
    {
      "sha": "d7697b7476c598ac4cdc612da6a219cb46cccb7e",
      "comment": "修复 x86 机器上 triton-ascend 3.2.x 导致 vLLM 启动时 `from triton.experimental import gluon` 失败的问题。在 vllm_ascend/__init__.py 中注册完整的 triton.experimental 父包和空 gluon 子模块，避免磁盘查找和 aarch64 上真实 gluon 与 3.2.0 core 不兼容的问题。风险低，但 stub 依赖 vLLM 仅 re-export 不调用 gluon 的假设。",
      "tags": [
        "bugfix",
        "low-risk",
        "patch",
        "platform"
      ],
      "test_impact": {
        "needs_test_update": true,
        "reason": "该修复依赖 import 顺序和 sys.modules 行为，建议增加单元测试模拟 x86/aarch64 两种 triton 布局，验证 vllm.triton_utils 可导入。",
        "suggested_test_areas": [
          "tests/ut/test_triton_stub.py"
        ]
      },
      "message": "[Ops][Bugfix] Fix triton stub on x86 machines (#14546)\n\n### What this PR does / why we need it?\n\nvLLM `main` (post-`v0.26.0`, PR vllm-project/vllm#48841) imports Gluon\nunconditionally when `HAS_TRITON` is true:\n\n```python\n# vllm/triton_utils/__init__.py\nif TYPE_CHECKING or HAS_TRITON:\n    from triton.experimental import gluon\n    from triton.experimental.gluon import language as gl\n    from triton.language.core import _aggregate as aggregate\n```\n\nOn Ascend, `triton-ascend==3.2.x` installs as the `triton` package, so\n`HAS_TRITON` is `True` and these imports execute. vLLM-Ascend already\nstubs the gluon symbols in `vllm_ascend/__init__.py`, but the stub only\nregisters the two **leaf** modules and is incomplete in two distinct,\narchitecture-specific ways:\n\n```python\nfor _gluon_stub in (\"triton.experimental.gluon\", \"triton.experimental.gluon.language\"):\n    if _gluon_stub not in sys.modules:\n        sys.modules[_gluon_stub] = ModuleType(_gluon_stub)\n```\n\n`triton-ascend 3.2.x` declares architecture-dependent triton\ndependencies (`triton-ascend/python/setup.py`,\n`ARCHITECTURE_DEPENDENCIES`): `triton==3.2.0` on x86_64, `triton==3.5.0`\non aarch64. This makes the same stub fail on **both** architectures, for\ndifferent reasons:\n\n- **x86_64 — `ModuleNotFoundError: No module named\n'triton.experimental'` (#13508).** triton 3.2.0 ships no `experimental/`\ndirectory at all. `from triton.experimental import gluon` resolves the\n**parent package** `triton.experimental` before the leaf (CPython\n`importlib._bootstrap._find_and_load_unlocked`: `if name in sys.modules:\nreturn`, then `_find_spec(name, parent.__path__)`). With only the leaves\npre-stubbed, the parent resolves to neither `sys.modules` nor disk and\nthe import raises. The pre-stubbed leaves are never consulted.\n\n- **aarch64 — `ImportError: cannot import name 'constexpr_type' from\n'triton.language.core'`.** triton 3.5.0 *does* ship a real\n`experimental/gluon/` (left on disk because triton-ascend's\n`get_packages()` does not include `triton/experimental`, so installing\ntriton-ascend does not overwrite the 3.5.0 leftovers). But that real\ngluon's `language/_layouts.py` imports `constexpr_type` from\n`triton.language.core` — a symbol added in **triton 3.4.0** — while\ntriton-ascend's `triton.language.core` is based on 3.2.0 and lacks it.\nSo loading the real gluon raises `ImportError`. The pre-existing\nleaf-only stub happened to mask this on aarch64 (the empty\n`triton.experimental.gluon` in `sys.modules` short-circuits the disk\nload via `if name in sys.modules: return`, so the real `__init__.py`\nnever executes) — but only by accident, and the stub still lacks the\nparent package the x86 path needs.\n\n**This PR** makes the stub register the **complete** parent→leaf\nhierarchy with empty modules, unconditionally on both architectures:\n\n```python\nif \"triton.experimental\" not in sys.modules:\n    _experimental = ModuleType(\"triton.experimental\")\n    _experimental.__path__ = []          # treated as a package -> _handle_fromlist runs\n    sys.modules[\"triton.experimental\"] = _experimental\nfor _gluon_stub in (\"triton.experimental.gluon\", \"triton.experimental.gluon.language\"):\n    if _gluon_stub not in sys.modules:\n        sys.modules[_gluon_stub] = ModuleType(_gluon_stub)\n```\n\n- On **x86_64**, registering the parent `triton.experimental` (with\n`__path__ = []`) lets `from triton.experimental import gluon`\nshort-circuit on `if name in sys.modules` and skip the disk lookup that\nfails — fixing #13508.\n- On **aarch64**, the empty leaves shadow the real\n(broken-against-3.2.0-core) gluon: `if name in sys.modules: return`\nreturns the empty module without executing the real `__init__.py`, so\nthe `constexpr_type` import is never reached. The empty parent is\nharmless (the real parent's `__init__.py` is also short-circuited).\n\nThe empty stubs are correct because vLLM only *imports* the gluon\nsymbols for re-export (`__all__`); no current code path invokes them, so\nan empty `ModuleType` is sufficient. (If a future triton-ascend\nbaselines on a triton that ships a gluon compatible with its own core,\nthis stub should be revisited — same as the existing `VLLM_VERSION !=\n\"0.26.0\"` gate is revisited each release.)\n\nThe `_aggregate` stub below is left unchanged (its `if\n_triton_available` + `try/except` + `hasattr` guard is already sound).\n\n- Fixes #13508\n\n### Does this PR introduce _any_ user-facing change?\n\nNo API or behavior change for end users. On x86_64 Ascend, vLLM startup\nthat previously crashed with `ModuleNotFoundError: No module named\n'triton.experimental'` now proceeds past the Gluon import. On aarch64,\nbehavior is unchanged from the pre-stub working state (startup already\nsucceeded there because the leaf-only stub masked the incompatible real\ngluon); this PR only makes the stub hierarchy complete and explicit so\nthe same code is correct on both architectures rather than relying on\nper-arch accident.\n\n### How was this patch tested?\n\n**1. Isolated matrix — 2 scenarios × 4 stub variants (no NPU\nrequired):**\n\nReproduces both architecture-specific failures and confirms the fix on\nboth. Scenarios are built by laying out a synthetic `triton/` package on\ndisk: x86_64 shape = no `experimental/` dir; aarch64 shape = real\n`experimental/gluon/` tree whose `_layouts.py` does `from\ntriton.language.core import ... constexpr_type` against a `core.py` that\nlacks it (mirroring triton-ascend 3.2.x).\n\n| stub variant | x86_64 (no experimental/) | aarch64 (real gluon, core\nlacks constexpr_type) |\n|---|---|---|\n| no stub (baseline) | FAIL `No module named 'triton.experimental'` |\nFAIL `cannot import name 'constexpr_type'` |\n| leaf-only (original upstream stub) | FAIL `No module named\n'triton.experimental'` | OK (masks real gluon by accident) |\n| `find_spec`-skip (first attempt) | OK | **FAIL `cannot import name\n'constexpr_type'`** ← aarch64 regression |\n| **this PR (full parent+leaf hierarchy)** | **OK** | **OK** |\n\nThe `find_spec`-skip variant (skip the stub when `triton.experimental`\nexists) was the first attempt — it fixed x86_64 but **regressed\naarch64** by no longer masking the real gluon, which then crashed on\n`constexpr_type`. That variant is explicitly rejected by this matrix.\n(This also corrects the suggestion in #13508 to \"prefer the real gluon\nwhen present\" — on triton-ascend 3.2.x the real gluon cannot load\nagainst the 3.2.0-based core, so it must be shadowed, not used.)\n\n**2. Confirming the shadow mechanism on the aarch64 shape:**\n\n```python\nimport sys\n# ... apply this PR's stub ...\nfrom triton.experimental import gluon\nfrom triton.experimental.gluon import language as gl\nassert \"triton.experimental.gluon.nvidia\" not in sys.modules   # real gluon __init__ never executed\n```\n\nThe real gluon's submodule `triton.experimental.gluon.nvidia` is absent\nfrom `sys.modules`, proving the empty stub short-circuited the disk load\nrather than loading-then-failing.\n\n**3. End-to-end on x86_64 A2 pod (DeepSeek-V4-Flash, TP=8):**\n\nWith this stub applied, `vllm serve` startup that previously ended at\n`vllm/triton_utils/__init__.py:15` (`from triton.experimental import\ngluon`) now proceeds past `vllm.triton_utils` into `vllm.config` /\n`vllm.engine.arg_utils`, and the model serves successfully. (A separate,\npre-existing deployment pitfall — running `vllm serve` from a cwd\ncontaining a `vllm/` subdir, which shadows the installed package with\n`(unknown location)` — was also resolved by changing the launch\ndirectory; that is a usage issue, not part of this code change.)\n\n- vLLM version: v0.27.1\n- vLLM main:\nhttps://github.com/vllm-project/vllm/commit/ba07e4a48fc951300d97eb506217dd530583dea3\n\n---------\n\nSigned-off-by: ChenxiQ <chenxi.qian.cq@outlook.com>"
    },
    {
      "sha": "3f83889b4b6c0da474a9604b51d8c78cf99bac11",
      "comment": "新增 Qwen3.6-35B-A3B-w8a8-310p-MTP 的 nightly 测试配置，并将 Qwen3.6-27B/35B 的 310P 配置从 weekly 迁移到 nightly，更新 workflow 和数据集列表。CI 配置变更，风险低。",
      "tags": [
        "ci",
        "test",
        "low-risk"
      ],
      "test_impact": {
        "needs_test_update": true,
        "reason": "新增 e2e 配置需要 CI 实际运行验证 baseline 和阈值；建议关注 MTP 场景的 accept rate 和 prefix cache 性能。",
        "suggested_test_areas": [
          "tests/e2e/nightly/single_node/models/configs/Qwen3.6-35B-A3B-w8a8-310p-MTP.yaml",
          ".github/workflows/configs/nightly_config.yaml",
          ".github/workflows/configs/weekly_config.yaml"
        ]
      },
      "message": "[CI] Qwen3.6-35B-A3B-w8a8-310p-MTP.yaml (#14221)\n\n### What this PR does / why we need it?\nwe add Qwen3.6-35B-A3B-w8a8-310p-MTP\n\n### Does this PR introduce _any_ user-facing change?\nno\n\n### How was this patch tested?\nrun case weekly\n\n- vLLM version: v0.27.1\n- vLLM main:\nhttps://github.com/vllm-project/vllm/commit/ba07e4a48fc951300d97eb506217dd530583dea3\n\n---------\n\nSigned-off-by: wanglei727 <naie.wanglei@h-partners.com>"
    },
    {
      "sha": "e645c56f1a2f79fcada0ad368f05cb5a04840abf",
      "comment": "CI 镜像构建并行度从 3 提升到 4，并将 Ubuntu 变体排在 openEuler 之前，以缩短常用镜像的构建等待时间。同时移除 fail-fast:false 和 concurrency 限制。纯 CI 调整，无运行时影响。",
      "tags": [
        "ci",
        "low-risk"
      ],
      "test_impact": {
        "needs_test_update": false,
        "reason": "CI 配置调整，无测试影响。",
        "suggested_test_areas": []
      },
      "message": "[CI] Raise image build parallelism to 4 and prioritize Ubuntu variants (#14854)\n\n## What this PR does / why we need it?\n\n- Raises `max-parallel` from 3 to 4 for both `image_build` and\n  `image_build_by_commit` matrix jobs, so more variants build\n  concurrently and total wall time drops when runners are available.\n- Reorders the matrix entries to list Ubuntu variants (A2, A3, 310P, A5)\n  before openEuler variants. With limited parallelism, the most widely\n  used Ubuntu images now finish first instead of being queued behind\n  openEuler builds.\n\n## Does this PR introduce _any_ user-facing change?\n\n- No. CI-only change; no runtime behavior is affected.\n\n## How was this patch tested?\n\n- CI verification only (workflow syntax checked by GitHub Actions\n  linter; no unit tests apply to workflow files).\n\n- vLLM version: v0.27.1\n- vLLM main:\nhttps://github.com/vllm-project/vllm/commit/ba07e4a48fc951300d97eb506217dd530583dea3\n\nSigned-off-by: qq_56278931 <wenshun3@h-partners.com>\nCo-authored-by: qq_56278931 <wenshun3@h-partners.com>"
    },
    {
      "sha": "49c4e5626d953248e122460c8b4b69d5d06ba5e6",
      "comment": "将 bisect 工具从『仅检查 vLLM 版本、不匹配则 SKIP』升级为『自动适配依赖版本』：从每个 vllm-ascend commit 读取 .github/vllm-release-tag.commit 与 requirements.txt/pyproject.toml 中的 torch-npu 版本，比较 good/bad 端点后对候选 commit 执行 vLLM checkout/editable install 或 pip 安装、torch-npu 强制重装。协调器新增 start.json 屏障，worker 在部署失败时发布不一致 ready 标记并等待 master 的 start/abort 决策，避免多节点部分部署后启动不完整测试。VersionPolicy 只在端点版本不同时启用检查，CANN 保持只读（换容器才能变更）。实现上删除了 vllm_compat.py，将版本发现/适配收敛到 version_compat.py，职责更清晰。潜在风险：vLLM 源码目录 checkout 失败、pip 安装失败都会转为 SKIP，可能掩盖真实回归；多节点协调新增的 wait_start 超时语义需要与 barrier_timeout 配合，若 master 在 publish_start 前崩溃，worker 会超时退出。整体仅影响 bisect 工具，不触及运行时。",
      "tags": [
        "feature",
        "ci",
        "low-risk",
        "tools/bisect"
      ],
      "test_impact": {
        "needs_test_update": true,
        "reason": "commit 已新增 test_version_compat.py 并扩展 test_coordinator.py，删除了旧 test_vllm_compat.py；但 runner/worker_agent 的版本适配与多节点 start 屏障缺少直接单测，且删除旧测试后需确认无遗留引用。",
        "suggested_test_areas": [
          "tests/ut/tools/bisect/test_version_compat.py",
          "tests/ut/tools/bisect/test_coordinator.py",
          "tests/ut/tools/bisect/ 下补充 runner/worker_agent 的版本适配与屏障交互测试"
        ]
      },
      "message": "[Feature][Bisect] Adapt dependency versions during history search (#13906)\n\n## What this PR does / why we need it?\n\nAdd dependency version adaptation to `tools/bisect` for nightly history\nsearches.\n\n- Read vLLM, torch-npu, and CANN versions from each `vllm-ascend`\ncommit.\n- Adapt vLLM and torch-npu when good/bad endpoints differ.\n- Keep CANN discovery read-only because changing CANN requires a new\ncontainer.\n- Synchronize version targets and the test start barrier across\nmulti-node workers.\n\n## Does this PR introduce any user-facing change?\n\nNo user-facing runtime change. This affects the bisect tool workflow\nonly.\n\n## How was this patch tested?\n\n- `FluGPU1`: `tests/ut/tools/bisect`: 57 passed.\n- Linux-native PR CI checks: Ruff, codespell, typos, markdownlint,\nforbidden-import checks, symbolic-meta checks, and related hooks passed.\n- `compileall` passed.\n\nThe full repository UT suite includes hardware/environment-dependent\npaths that are not part of this bisect-tool change.\n\n- vLLM version: v0.27.1\n- vLLM main:\nhttps://github.com/vllm-project/vllm/commit/58d3918e3ea0a544ffedadad2ba84559e9c51d8f\n\n---------\n\nSigned-off-by: NaraFluorine <a3275529098@outlook.com>\nCo-authored-by: gemini-code-assist[bot] <176961590+gemini-code-assist[bot]@users.noreply.github.com>"
    },
    {
      "sha": "12276de8059151c56c876154abc536b92d8681e4",
      "comment": "在 NPUPlatform.check_and_update_config 中新增 _validate_indexer_pp_config，对 PP size > 1 的模型校验 IndexShare（indexer_types full/shared）与 IndexCache（use_index_cache）的跨 stage 依赖。核心逻辑：对每个 PP rank 的起始层，若该层是 shared Indexer 且本 stage 内没有前置 full Indexer，或该层因 index_topk_pattern/freq 跳过 Top-K 计算，则抛出 ValueError。这防止了跨 PP 的 Top-K index 传播在无支持时静默产生错误结果。实现上通过 get_pp_indices 计算层归属，兼容 VLLM_PP_LAYER_PARTITION 环境变量（测试中 patch 为 None）。风险较低，属于提前失败式校验，但需注意 indexer_types 长度小于 num_hidden_layers 时的容错（layer_id >= len 时视为 None），以及 use_index_cache 与 indexer_types 同时存在时的校验顺序。",
      "tags": [
        "feature",
        "low-risk",
        "platform",
        "pipeline-parallel"
      ],
      "test_impact": {
        "needs_test_update": true,
        "reason": "commit 已在 tests/ut/test_platform.py 新增 4 个测试覆盖 shared 越界、合法对齐、IndexCache 两种 pattern、以及 check_and_update_config 调用时机；但未覆盖 indexer_types 长度不足、use_index_cache 与 indexer_types 同时存在、VLLM_PP_LAYER_PARTITION 自定义分区等边界。",
        "suggested_test_areas": [
          "tests/ut/test_platform.py",
          "补充 VLLM_PP_LAYER_PARTITION 自定义分区与混合 IndexShare/IndexCache 场景"
        ]
      },
      "message": "[Feature] Validate IndexShare and IndexCache PP partition (#14088)\n\n### What this PR does / why we need it?\nAdd PP boundary validation for IndexShare and runtime IndexCache\nconfigurations. The validation rejects stages that start with a shared\nor cached Top-K index when cross-PP index propagation is unsupported.\nExisting _skip_topk runtime behavior remains unchanged.\n### Does this PR introduce _any_ user-facing change?\nNone\n### How was this patch tested?\n\n\n- vLLM version: v0.27.1\n- vLLM main:\nhttps://github.com/vllm-project/vllm/commit/ba07e4a48fc951300d97eb506217dd530583dea3\n\n---------\n\nSigned-off-by: YangShuai52 <shuaiyang047@163.com>"
    },
    {
      "sha": "2497666c93b222a98c190f89172b1b1fce3d9daf",
      "comment": "针对 EAGLE3 draft 使用全目标词表（draft_vocab_size == vocab_size）的场景，在 draft 模型加载后立即将 draft_id_to_target_id 置为 None，使 compute_logits 走 fast path 直接返回 logits，避免每次调用都构建 full-vocab -inf buffer 并通过 identity 映射 scatter（实测 ~1.4ms/op，约 55 倍于纯 copy）。实现上 _maybe_remove_d2t 仅对 method == 'eagle3' 生效，且通过 get_vocab_size() 与 draft_model.config.draft_vocab_size 比较。风险点：依赖上游 compute_logits 对 draft_id_to_target_id 为 None 的既有处理路径；若后续代码在其它地方（如概率采样、vocab remapping 判断）假设该属性存在，可能引入隐性依赖。当前 529b 的 compute_draft_token_ids 已用 hasattr 且 is None 判断，说明兼容性已建立。",
      "tags": [
        "bugfix",
        "performance",
        "low-risk",
        "spec-decode"
      ],
      "test_impact": {
        "needs_test_update": true,
        "reason": "commit 未新增测试。需要验证 full-vocab EAGLE3 draft 加载后 draft_id_to_target_id 被置 None、compute_logits 返回原 logits，以及 reduced-vocab 场景不受影响。",
        "suggested_test_areas": [
          "tests/ut/spec_decode/a2/test_eagle_proposer.py",
          "新增 full-vocab draft 的 d2t 移除与 logits fast path 测试"
        ]
      },
      "message": "[BugFix][Performance] Skip identity d2t mapping for full-vocab drafts (#14738)\n\n### What this PR does / why we need it?\n\nWhen the draft model uses the full target vocabulary (`draft_vocab_size\n== vocab_size`), the `draft_id_to_target_id` mapping is the identity.\nThe draft's `compute_logits` still builds a full-vocab `-inf` buffer and\nscatters logits through this identity mapping on every call, which is\npure overhead: measured ~1.4 ms per call at (112, 151936) bf16 — ~55x\nthe cost of a plain copy, and comparable to the lm_head matmul itself.\n\nThis drops the mapping right after the draft model loads when the vocab\nsizes are equal, so `compute_logits` takes the fast path and returns\nlogits unchanged.\n\n### Does this PR introduce any user-facing change?\n\nNo.\n\n### How was this patch tested?\n\n1. Verified with a Qwen3-8B target and the EAGLE3 draft\n`Qwen3-8B-speculator-eagle3`: the live `draft_id_to_target_id` table\ndumped from a real run equals `arange` (identity), and the scatter\noutput is bit-identical to its input for real tensors, so skipping it is\na no-op transform.\n2. A micro-benchmark of the removed op shows `logits_new[:, targets] =\nlogits` at (112, 151936) bf16 takes ~1.42 ms/op, vs 0.019 ms for a plain\n`copy_`.\n\n- vLLM version: v0.27.1\n- vLLM main:\nhttps://github.com/vllm-project/vllm/commit/ba07e4a48fc951300d97eb506217dd530583dea3\n\nSigned-off-by: drslark <slarksblood@qq.com>\nCo-authored-by: GLM-5.3 <glm@z.ai>"
    },
    {
      "sha": "2ffcca0af23f9df20455def8205cd210a24b8e06",
      "comment": "为 AscendStore 的 Mooncake 后端增加 tenant_id 支持，对齐 vLLM PR #48069 的租户传播行为。MooncakeStoreConfig 新增 tenant_id 字段，_normalize_tenant_id 将缺失/null/空白归一为 'default' 并 trim 非空值，非字符串抛 TypeError。_setup_store 中仅当 tenant_id != 'default' 时才向 MooncakeDistributedStore.setup() 传递 tenant_id 关键字，保持旧版 Mooncake（0.3.11.post1）默认路径兼容；FabricMem 与非 FabricMem 两条路径均生效，SSD offload 参数原样保留。文档补充了严格多租户模式、quota 配置与部署约束。风险：非 default tenant 要求 Mooncake >= 0.3.12，若底层 setup() 不支持 tenant_id 会抛 TypeError（测试中显式保留了该传播），用户需自行确保版本匹配；tenant_id 是实例级命名空间而非认证机制，文档已明确。",
      "tags": [
        "feature",
        "medium-risk",
        "distributed",
        "kv-offload"
      ],
      "test_impact": {
        "needs_test_update": true,
        "reason": "commit 已新增 TestMooncakeBackendSetup 覆盖 tenant 归一化、default 省略、非 default 转发（FabricMem/非 FabricMem）、SSD kwargs 保留、scheduler-only 路径与 TypeError 传播；但未覆盖 mooncake.json 中 tenant_id 为数字/列表等非字符串非 null 的解析层测试（当前测试直接构造 config，未走 from_file）。",
        "suggested_test_areas": [
          "tests/ut/distributed/ascend_store/test_backend.py",
          "补充 MooncakeStoreConfig.from_file 对非法 tenant_id 类型的解析测试"
        ]
      },
      "message": "[Feature][KV Pool] Add Mooncake tenant ID support (#14823)\n\n### What this PR does / why we need it?\n\nThis PR adds Mooncake tenant ID support to the vLLM-Ascend AscendStore\nbackend, following the tenant propagation behavior introduced by [vLLM\nPR #48069](https://github.com/vllm-project/vllm/pull/48069).\n\n- Read an optional `tenant_id` from `mooncake.json`.\n- Normalize a missing, `null`, empty, or whitespace-only value to\n`default`, and trim surrounding whitespace from non-empty strings.\n- Reject non-string, non-null tenant values.\n- Preserve backward compatibility by omitting the `tenant_id` keyword\nwhen the normalized value is `default`.\n- Forward a non-default tenant to `MooncakeDistributedStore.setup()` in\nboth FabricMem and non-FabricMem paths without silently falling back to\n`default`.\n- Preserve the existing SSD offload setup arguments while adding the\ntenant argument.\n- Document strict Mooncake multi-tenant mode, tenant quota\nconfiguration, and deployment constraints.\n\nThe tenant ID is an instance-level namespace and quota identity. It is\nnot an authentication mechanism or a request-level tenant selector.\nPrefill, Decode, scheduler, and replica instances that share KV entries\nmust use the same tenant ID.\n\n### Does this PR introduce _any_ user-facing change?\n\nYes. `mooncake.json` accepts a new optional `tenant_id` field.\n\nThe default behavior is unchanged when the field is omitted or resolves\nto `default`. A non-default tenant requires a Mooncake binding that\nsupports `MooncakeDistributedStore.setup(..., tenant_id=...)`. Strict\nnamespace isolation and quota admission also require Mooncake Master\nmulti-tenant mode and a registered tenant quota policy.\n\n### How was this patch tested?\n\nStatic and mocked unit checks on the rebased head:\n\n```bash\npython -m py_compile \\\n  vllm_ascend/distributed/kv_transfer/kv_pool/ascend_store/backend/mooncake_backend.py \\\n  tests/ut/distributed/ascend_store/test_backend.py\n\npre-commit run ruff-check --files \\\n  vllm_ascend/distributed/kv_transfer/kv_pool/ascend_store/backend/mooncake_backend.py \\\n  tests/ut/distributed/ascend_store/test_backend.py\n\npre-commit run ruff-format --files \\\n  vllm_ascend/distributed/kv_transfer/kv_pool/ascend_store/backend/mooncake_backend.py \\\n  tests/ut/distributed/ascend_store/test_backend.py\n\npre-commit run codespell --files \\\n  vllm_ascend/distributed/kv_transfer/kv_pool/ascend_store/backend/mooncake_backend.py \\\n  tests/ut/distributed/ascend_store/test_backend.py \\\n  docs/source/user_guide/feature_guide/kv_pool.md\n\npre-commit run typos --files \\\n  vllm_ascend/distributed/kv_transfer/kv_pool/ascend_store/backend/mooncake_backend.py \\\n  tests/ut/distributed/ascend_store/test_backend.py \\\n  docs/source/user_guide/feature_guide/kv_pool.md\n\ngit diff --check upstream/main...HEAD\n```\n\nResults:\n\n- Python compilation, Ruff check, Ruff format check, codespell, typos,\nand diff check passed.\n- The Mooncake backend mock test classes passed: 14 tests covering\ntenant normalization and validation, default omission, non-default\nforwarding in FabricMem and non-FabricMem paths, scheduler-only setup,\nSSD kwargs preservation, and original `TypeError` propagation.\n- AscendStore unit tests were also validated in a Linux environment on\nvLLM-Ascend `508e5a8ce3cc8bbf2684bb6c7c47186fd61a8532` with vLLM\n`58d3918e3ea0a544ffedadad2ba84559e9c51d8f`.\n\nA3 end-to-end validation used:\n\n- vLLM-Ascend: `508e5a8ce3cc8bbf2684bb6c7c47186fd61a8532`\n- vLLM: `58d3918e3ea0a544ffedadad2ba84559e9c51d8f`\n\nValidated scenarios:\n\n- A new `tenant-a` Prefill/Decode deployment missed and stored KV in\nMooncake.\n- A fresh `tenant-a` deployment externally hit the stored KV.\n- A fresh `tenant-b` deployment did not read `tenant-a` KV for the same\ntoken IDs.\n- A small-quota tenant write was rejected by Mooncake Master and\nattributed to the correct tenant.\n- Omitting `tenant_id` completed a compatible default-tenant startup and\nrequest.\n- FabricMem, SSD offload, and a non-default tenant were enabled\ntogether; Store PUT/GET succeeded and SSD setup parameters were\npreserved.\n\nThe SSD validation confirms successful Store PUT/GET with the combined\nFabricMem, SSD, and tenant configuration. It does not distinguish\nwhether the observed GET was served from FabricMem or the SSD tier.\n\n- vLLM version: v0.27.1\n- vLLM main:\nhttps://github.com/vllm-project/vllm/commit/cdc4824a21eaa986d4d1fee90a7e6465c9f706e6\n\nSigned-off-by: lhecust721 <lhecust721@users.noreply.github.com>\nCo-authored-by: lhecust721 <lhecust721@users.noreply.github.com>"
    },
    {
      "sha": "c073c3b7587e03bbe78a6078ffc20b1343795b21",
      "comment": "修复 level-2 sleep/wake 后 MoE ALLTOALL dispatcher 的 expert_ids_per_ep_rank 与 DeepSeek-V4 DSA Hadamard 矩阵失效的问题。根因是这些张量由非 nn.Module 运行时对象持有，level-2 的 buffer 备份路径（model.named_buffers()）发现不了它们。方案：新增 get_or_register_attention_buffer 工具，将 Hadamard 注册为 Attention 模块的共享 non-persistent buffer；AscendMoERunner 将 expert_ids_per_ep_rank 注册到 routed_experts 的 non-persistent buffer。worker.sleep 相应调整：level-1 只显式保存 _dsa_cp_hadamard/_dsa_hadamard 两个控制 buffer（因为它们在 weights mem-pool 外创建），level-2 仍保存全部 buffers。persistent=False 保证不进入 checkpoint。风险：get_or_register_attention_buffer 要求 static_forward_context 中存在 layer_names 对应的 nn.Module，若某 DSA 场景未注册 layer 会直接 ValueError；多个 module 共享同一 buffer 时，若已有不同 tensor 会报错，避免静默不一致。sleep level-1 的 _allowed_names 匹配 buffer 名尾段，若未来新增类似控制 buffer 需同步扩展。",
      "tags": [
        "bugfix",
        "medium-risk",
        "attention",
        "moe",
        "sleep-mode"
      ],
      "test_impact": {
        "needs_test_update": true,
        "reason": "commit 已更新 test_attention_utils.py（get_or_register_attention_buffer 单测）和 test_worker_v1.py（sleep 时 mock named_buffers）；但缺少 level-2 wake 后 buffer 值恢复的端到端验证，以及 MoE expert_ids_per_ep_rank 注册后 named_buffers 包含该 tensor 的断言。",
        "suggested_test_areas": [
          "tests/ut/attention/test_attention_utils.py",
          "tests/ut/worker/a2/test_worker_v1.py",
          "补充 sleep/wake 后 DSA Hadamard 与 expert_ids_per_ep_rank 恢复的集成测试"
        ]
      },
      "message": "[BugFix] Restore MoE and DSA control buffers after level-2 wake (#13352)\n\n## Summary\n\nRestore runtime control tensors through model buffers so that level-2\nsleep/wake saves and restores them via `model.named_buffers()`.\n\n- Register the DeepSeek-V4 DSA Hadamard matrices as shared,\nnon-persistent buffers on their owning Attention modules.\n- Register the ALLTOALL dispatcher's `expert_ids_per_ep_rank` tensor as\na non-persistent buffer on `routed_experts`.\n\n## Motivation\n\nThese tensors are created or owned by non-`nn.Module` runtime objects,\nso they are not discovered by the level-2 buffer backup path. After\nwake-up, the original owners could therefore retain invalid device\nstorage.\n\nThis change keeps the same Tensor objects used by the runtime\ncomponents, but makes an appropriate model module own each one as a\nbuffer. `persistent=False` keeps this runtime-derived state out of model\ncheckpoints.\n\n## Scope\n\n- Affects DeepSeek-V4 DSA attention Hadamard matrices and ALLTOALL\ndispatcher expert-ID mapping only.\n- Does not change model weights, checkpoint contents, or ALLTOALL\ndispatch semantics.\n\n## Testing\n\n- `python -m compileall` for the four modified Python files.\n\n- vLLM version: v0.27.1\n- vLLM main:\nhttps://github.com/vllm-project/vllm/commit/cdc4824a21eaa986d4d1fee90a7e6465c9f706e6\n\n---------\n\nSigned-off-by: Ulrica111 <ulrica111@users.noreply.github.com>\nCo-authored-by: Ulrica111 <ulrica111@users.noreply.github.com>"
    },
    {
      "sha": "529b455750a2918683b335629276f3ddf03a4e99",
      "comment": "为 MRV1 推测解码启用 probabilistic draft sampling（draft_sample_method='probabilistic'），对齐 vLLM PR #40269。核心改动：compute_draft_token_ids 返回值从 Tensor 变为 (tokens, probs) 二元组，新增 _sample_draft_from_logits 委托上游 _sample_from_logits 处理温度对齐与 Gumbel-max 采样；sampling_metadata 沿 _run_merged_draft 各分支传递；DSpark 移除 probabilistic 限制，并在自回归循环中逐 token 概率采样、stack 成 [num_blk, K, V]；model_runner_v1 中 draft_probs 获取不再限制 PP world_size > 1，且每次 execute_model 前重置 _draft_probs 防止 stale 数据。风险较高：compute_draft_token_ids 签名变更影响所有调用点（eagle/dflash/dspark/mtp/step3p5/310p），测试虽已更新但概率路径的 TP 对齐（_align_tensor_and_indices 后 probs 截断）、vocab remapping 回退 greedy、dummy_run 时 sampling_metadata 为 None 的 fallback 等分支仍需更多验证。PR 描述中提到的 logger.warning_once 在标准 logger 上不存在的问题，代码中仍在使用，需确认 vllm.logger 提供了该方法。",
      "tags": [
        "feature",
        "bugfix",
        "high-risk",
        "spec-decode",
        "model-runner"
      ],
      "test_impact": {
        "needs_test_update": true,
        "reason": "commit 已更新多个测试（310p 签名、eagle 签名、dflash2 返回 tuple、dspark 接受 probabilistic），但缺少对 probabilistic 路径的端到端验证：sampling_metadata 传递、draft_probs 形状/对齐、TP 下 probs 截断、vocab remapping 回退 greedy、dummy_run fallback 等均未直接覆盖。",
        "suggested_test_areas": [
          "tests/ut/spec_decode/a2/test_eagle_proposer.py",
          "tests/ut/spec_decode/test_dspark_proposer.py",
          "tests/ut/spec_decode/test_dflash2_proposer.py",
          "新增 probabilistic sampling 的端到端/集成测试（含 TP 与 vocab remapping 场景）"
        ]
      },
      "message": "[Bugfix][SpecDecode] Support probabilistic draft sampling on MRV1 (#14485)\n\n### What this PR does / why we need it?\nvllm：https://github.com/vllm-project/vllm/pull/40269\nThis pull request enables support for probabilistic draft sampling\n(`draft_sample_method='probabilistic'`) in Ascend speculative decoding.\nIt updates the proposer classes (`llm_base_proposer.py`, `step3p5.py`)\nto accept and propagate `sampling_metadata` to retrieve draft\nprobabilities, and integrates this with the model runner. Additionally,\nit removes the previous restriction on DSpark probabilistic draft\nsampling.\n\nFeedback on the implementation:\n- In `llm_base_proposer.py`, `logger.warning_once` is called but does\nnot exist on standard loggers, which will cause an `AttributeError` at\nruntime.\n- In `llm_base_proposer.py`, `draft_probs_step0` must be properly\naligned using `_align_tensor_and_indices` when tensor parallelism is\nenabled to avoid shape mismatches.\n\n### Does this PR introduce _any_ user-facing change?\nYes, users can now use `draft_sample_method='probabilistic'` with Ascend\nspeculative decoding.\n\n### How was this patch tested?\nTested via existing speculative decoding tests and verified with the new\nprobabilistic sampling path.\n\n### How was this patch tested?\npass\nhttps://github.com/vllm-project/vllm-ascend/actions/runs/32640733392/job/97209084690?pr=14485\n\n\nhttps://github.com/vllm-project/vllm-ascend/actions/runs/32471684844/job/96741786987?pr=14485\n\n\n- vLLM version: v0.27.1\n- vLLM main:\nhttps://github.com/vllm-project/vllm/commit/ba07e4a48fc951300d97eb506217dd530583dea3\n\n---------\n\nSigned-off-by: liyishi <1252651434@qq.com>"
    },
    {
      "sha": "fa668c6495f21734dfd3b7aab464ac73beb4d28b",
      "comment": "将 .pre-commit-config.yaml 中多个自定义 hook 的 entry 从 python 改为 python3，修复 pre-commit 在部分环境（python 指向 python2 或不存在）下误报/无法执行的问题。涉及 python-init、check-forbidden-imports、check-boolean-context-manager、check-long-functions、check-symbolic-meta 五个 hook。纯 CI 配置变更，无运行时影响，风险极低。",
      "tags": [
        "ci",
        "chore",
        "low-risk"
      ],
      "test_impact": {
        "needs_test_update": false,
        "reason": "仅修改 pre-commit hook 的 entry 命令，不涉及业务代码或测试逻辑，现有 CI 即可验证。",
        "suggested_test_areas": []
      },
      "message": "[CI] fix pre-commit python3 bug (#14646)\n\n### What this PR does / why we need it?\nfix: pre-commit incorrectly flags valid Python 3 code\n\n### Does this PR introduce _any_ user-facing change?\n\navoid unneeded pre-commit recognition\n\n### How was this patch tested?\n\n- vLLM version: v0.27.1\n- vLLM main:\nhttps://github.com/vllm-project/vllm/commit/58d3918e3ea0a544ffedadad2ba84559e9c51d8f\n\n---------\n\nSigned-off-by: xqchen7 <chenxueqing7@huawei.com>"
    },
    {
      "sha": "07482eb320ff036cedf06fd4f2149346443b1dbd",
      "comment": "Recompute scheduler 大规模同步 vLLM 0.27.1 main2main 语义，改动密集且涉及核心调度路径。主要变更：(1) 新增 _truncate_computed_blocks_for_connector 及 Mamba clamp 变体，处理 legacy 短 Mamba 组在 KV connector 截断时的越界问题；(2) preemption 逻辑重构：PRIORITY 策略下先选出 victim 再统一走 _preempt_request，并新增 drop_stale_output 参数，非 producer 场景将 recomputed_req 与 preempted_req 合并；(3) 引入 stale output 处理：num_stale_output_tokens > 0 且未 drop 时跳过调度，update_from_output 中 output_is_stale 时跳过 spec 计数与 num_computed_tokens 回退；(4) connector 匹配改为 block-aligned local tokens，partial tail 时截断本地 blocks 或禁用外部 KV；(5) scheduler_output 新增 partial_tail_offloads、scheduled_encoder_input_stats、ec_manager_metadata 等字段；(6) prefill_stats 仅在首次 prefill 且 emit output 时 finalize。风险极高：preemption 分支的 victim_index 调整、stale output 与 spec decode 计数交互、partial tail 与 connector 的边界条件都容易引入回归，且当前仅新增一个 Mamba 截断测试，覆盖严重不足。",
      "tags": [
        "refactor",
        "high-risk",
        "scheduler",
        "kv-offload"
      ],
      "test_impact": {
        "needs_test_update": true,
        "reason": "commit 仅新增 test_truncate_computed_blocks_supports_legacy_short_mamba_group 一个测试，而 preemption 重构、stale output、partial tail offload、spec 计数跳过等核心逻辑均无直接测试覆盖，现有 test_recompute_scheduler.py 不足以防止回归。",
        "suggested_test_areas": [
          "tests/ut/core/test_recompute_scheduler.py",
          "补充 PRIORITY preemption victim 选择与 scheduled_running_reqs 一致性测试",
          "补充 stale output 与 spec decode 计数交互测试",
          "补充 partial tail 与 KV connector 的 block-aligned 截断测试",
          "补充 scheduler_output 新增字段（partial_tail_offloads/ec_manager_metadata）的序列化/消费测试"
        ]
      },
      "message": "[Misc][Core][P/D] Add main2main 0.27.1 for Recompute scheduler (#14504)\n\n### What this PR does / why we need it?\nAdd main2main 0.27.1 for Recompute scheduler.\n\n### Does this PR introduce _any_ user-facing change?\nNo.\n\n### How was this patch tested?\nBy CI.\n\n- vLLM version: v0.27.1\n- vLLM main:\nhttps://github.com/vllm-project/vllm/commit/ba07e4a48fc951300d97eb506217dd530583dea3\n\nSigned-off-by: nwpu-zxr <zhouxuerong2@huawei.com>"
    },
    {
      "sha": "24d79e69ef9ece05fc4152d75041e478421a0af6",
      "comment": "nightly/weekly 结果后处理脚本 result_postprocess.py 增加 case_key 写入：merge_postprocess_payload 新增 case_key 参数并写入 testEnv.case_key，同时移除对 testcase_info.Testcase_Name 的覆盖，保留预设 JSON 值。这解决了 _benchmarks 下多 case（如 acc_aime2025、perf）时 case 标识丢失的问题。改动小、风险低，但 postprocess 是 CI 结果上报链路的一部分，若 case_key 缺失或 Testcase_Name 被意外覆盖会影响看板数据准确性。",
      "tags": [
        "ci",
        "test",
        "low-risk"
      ],
      "test_impact": {
        "needs_test_update": true,
        "reason": "commit 未新增测试。需要验证 merge_postprocess_payload 写入 testEnv.case_key 且不再覆盖 testcase_info.Testcase_Name，以及 postprocess_one_benchmark 正确传递 case_key。",
        "suggested_test_areas": [
          "tests/e2e/nightly/ 下 result_postprocess 相关测试",
          "新增 case_key 写入与 Testcase_Name 保留的断言"
        ]
      },
      "message": "[CI][Test] Add case_key to postprocess testEnv (#14498)\n\n### What this PR does / why we need it?\nNightly/weekly YAML configs under `_benchmarks` may define multiple\ncases (e.g. `acc_aime2025`, `perf`). The case key was already passed\ninto result postprocess for output filenames, but was not written into\nthe uploaded JSON.\n\nThis PR:\n- Writes `testEnv.case_key` with the `_benchmarks` case name\n- Stops overwriting `testcase_info.Testcase_Name`, so the preset JSON\nvalue is preserved\n\n### Does this PR introduce _any_ user-facing change?\nNo\n\n### How was this patch tested?\nby running a test\n\n- vLLM version: v0.27.1\n- vLLM main:\nhttps://github.com/vllm-project/vllm/commit/58d3918e3ea0a544ffedadad2ba84559e9c51d8f\n\nSigned-off-by: jiangyunfan1 <jiangyunfan1@h-partners.com>"
    },
    {
      "sha": "37e382498c81fdbcfce8529ea1d53570e0d239a5",
      "comment": "回滚 #14008『MRV2 支持基础 MLA prefill context parallelism』。删除 AscendMLAPCPMetadata、AscendMLAPCPMetadataBuilder、AscendMLAPCPImpl 及 mla_v1.py 中相关分支（get_builder_cls/get_impl_cls 不再检查 enable_pcp，exec_kv_prefill 移除 attn_metadata 参数，mla_preprocess_prefill 恢复按 num_actual_tokens 截断），同时删除 tests/ut/attention/a2/test_mla_v1.py 与 tests/ut/worker/test_attn_utils_v2.py 中的 PCP 测试。回滚后 MRV2 MLA 仅支持 DCP 与标准路径，PCP 用户需回退到旧版本或等待重新实现。风险中等：回滚本身恢复已知稳定状态，但需确认没有其它模块（如 attn_utils、context_parallel 配置）残留对 AscendMLAPCP* 的引用；另外 mla_v1.py 中 split_decodes_and_prefills 的 treat_short_extends_as_decodes 参数从『PCP 启用时 False』改为 common_attn_metadata.context_parallel_metadata is None，语义变化需验证非 CP 场景行为一致。",
      "tags": [
        "revert",
        "medium-risk",
        "attention",
        "mla"
      ],
      "test_impact": {
        "needs_test_update": true,
        "reason": "commit 删除了 PCP 相关测试（test_mla_v1.py 中 PCP 用例、test_attn_utils_v2.py 中 prefill state 传播用例），需确认剩余 MLA 测试完整覆盖非 PCP 路径，并检查是否有其它测试文件仍引用已删除的 AscendMLAPCP* 符号。",
        "suggested_test_areas": [
          "tests/ut/attention/a2/test_mla_v1.py",
          "tests/ut/worker/test_attn_utils_v2.py",
          "全局搜索 AscendMLAPCP 残留引用并清理"
        ]
      },
      "message": "Revert \"[Feature][MRV2] Support basic MLA prefill context parallelism\" (#14853)\n\nReverts vllm-project/vllm-ascend#14008 \n\n- vLLM version: v0.27.1\n- vLLM main:\nhttps://github.com/vllm-project/vllm/commit/ba07e4a48fc951300d97eb506217dd530583dea3\n\nSigned-off-by: wzx0726 <zhexuanwu12@gmail.com>"
    }
  ],
  "architecture_based_on_sha": "e04f251a5895969d1c1f4ac9a3807177a0c4769c",
  "architecture_generated_at": "2026-08-01T20:25:03.693997+08:00"
}