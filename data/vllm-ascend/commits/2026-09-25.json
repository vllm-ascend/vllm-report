{
  "date": "2026-09-25",
  "repo": "vllm-project/vllm-ascend",
  "branch": "main",
  "commits": [
    {
      "sha": "2bb3f44716f3505d2723a4e5badb10211a6c5589",
      "author": {
        "name": "SILONG ZENG",
        "email": "2609716663@qq.com"
      },
      "date": "2026-09-25T18:27:38+08:00",
      "message": "[CI] Improve nightly image build and publishing workflow (#17523)\n\n### What this PR does / why we need it?\nThe manually triggered nightly image build workflow previously supported\nbuilding a specific vLLM commit together with a specific vLLM Ascend\ncommit. However, it had several limitations:\n\n1. `vllm_commit` only accepted a commit hash and could not directly use\na vLLM release tag such as `v0.29.0`.\n2. Temporary image names truncated the vLLM revision to seven\ncharacters. This is appropriate for commit hashes, but truncates release\ntags and may cause ambiguous image names.\n3. The A5 Dockerfiles always cloned vLLM by tag and did not explicitly\nsupport checking out an exact vLLM commit.\n4. Images built from specified commits were limited to the temporary\nrepository, making it difficult to reuse them as the base image for\nsubsequent `nightly-ci-*` builds.\n5. The separate `tag` and `branch` workflow inputs consumed two\nmanual-dispatch input slots and made it difficult to add an explicit\npublishing option.\n\nThis PR updates the nightly image build workflow so that a complete\npipeline can be run with specified vLLM and vLLM Ascend revisions:\n\n1. Build the base image from the specified revisions.\n2. Optionally publish a daily build using the release-compatible\n`nightly-main[-suffix]` tags.\n3. Reuse that image when building `nightly-ci-main-a2`,\n`nightly-ci-main-a3`, and other nightly CI images.\n4. Run nightly tests against the same vLLM Ascend revision contained in\nthe selected base image.\n\n#### Support vLLM release tags in commit mode\n\nThe `vllm_commit` workflow input now accepts either:\n\n- A 7–40 character hexadecimal commit hash.\n- A vLLM release tag such as `v0.29.0`, `v0.30.0rc1`, or a supported\npost/dev release tag.\n\nThe image build passes only one corresponding Docker build argument:\n\n- `VLLM_COMMIT` when a commit hash is provided.\n- `VLLM_TAG` when a release tag is provided.\n\nThis allows release builds to preserve the correct vLLM version\nmetadata, which is required by version-dependent code paths such as\n`vllm_version_is()`.\n\nThe `vllm_ascend_commit` input remains an exact commit hash so that the\nselected vLLM Ascend source revision is deterministic.\n\n#### Support exact vLLM commits in A5 Dockerfiles\n\n`Dockerfile.a5` and `Dockerfile.a5.openEuler` now accept an optional\n`VLLM_COMMIT` build argument.\n\nWhen `VLLM_COMMIT` is provided, the Dockerfile initializes the vLLM\nrepository, fetches the specified commit, and checks out `FETCH_HEAD`.\nOtherwise, it keeps the existing tag-based shallow clone behavior using\n`VLLM_TAG`.\n\nThis keeps the default A5 build behavior unchanged while ensuring that\ncommit-mode builds actually use the requested vLLM commit.\n\n#### Preserve complete vLLM release tags in temporary image names\n\nFor commit hashes, temporary image names continue to use the first seven\ncharacters.\n\nFor release tags, the complete tag is retained. For example:\n\n```text\nvllm_commit=v0.29.0\nvllm_ascend_commit=abcdef123456...\n\nvllm-ascend-v0.29.0-abcdef1\n```\n\nThis prevents release tags from being truncated and avoids collisions\nbetween versions that share the same initial characters.\n\n#### Combine Docker branch and tag inputs\n\nThe separate `tag` and `branch` workflow inputs are replaced by a single\n`docker_ref` choice input.\n\nThe values are interpreted as follows:\n\n- `main` and `releases/*` select a branch.\n- `v*` selects a release tag.\n- `tag:main` preserves the previous manual Docker tag behavior for\n`main`.\n- `none` enables commit mode when both revision inputs are provided.\n\nThis preserves the existing branch and tag build paths while freeing a\nworkflow-dispatch input slot for the publishing option.\n\nExternal scripts that manually dispatch the workflow must replace the\nold `tag` or `branch` parameter with `docker_ref`.\n\n#### Publish specified revisions for downstream nightly CI builds\n\nCommit-mode builds now follow these publishing rules:\n\n| Build type | `publish_nightly_main` | Result |\n| --- | --- | --- |\n| `release` | `false` or `true` | Publish using `nightly-main[-suffix]`\n|\n| `daily` | `false` | Keep the existing temporary-only behavior |\n| `daily` | `true` | Publish using the release-compatible\n`nightly-main[-suffix]` tags |\n\nThe publishing switch only changes daily commit-mode behavior. Release\ncommit builds already publish their final image and therefore do not\ndepend on this option.\n\nWhen `publish_nightly_main` is enabled for a daily commit build:\n\n- The image is still built with the selected daily dependencies and CANN\nimage.\n- The final multi-architecture image is published to the same repository\nused by release nightly images.\n- The final image uses the same `nightly-main[-suffix]` naming\nconvention as a release build.\n- The dated daily tag is not used as the final published tag for this\npromotion path.\n\nFor example:\n\n```text\nnightly-main\nnightly-main-a3\nnightly-main-310p\nnightly-main-a5\nnightly-main-openeuler\nnightly-main-a3-openeuler\n```\n\nThis is required because release-mode nightly CI image builds use images\nsuch as:\n\n```text\nswr.cn-north-12.myhuaweicloud.com/ascend/vllm-ascend:nightly-main\n```\n\nas their base image. Publishing a daily build only with a CANN-versioned\nand dated tag would not be picked up by the subsequent `nightly-ci-*`\nworkflow.\n\nAfter promotion, manually triggering a nightly CI build with\n`build_type=release` produces images such as:\n\n```text\nnightly-ci-main-a2\nnightly-ci-main-a3\nnightly-ci-main-310p\nnightly-ci-main-a5\n```\n\nThese images inherit the exact vLLM and vLLM Ascend revisions already\ncontained in the promoted `nightly-main` base image. The downstream\nDocker build does not replace them with the latest vLLM Ascend `main`\nsource.\n\n### Does this PR introduce _any_ user-facing change?\n\nYes. The manual workflow dispatch interface changes in the following\nways:\n\n- The previous `tag` and `branch` inputs are replaced by `docker_ref`.\n- `vllm_commit` accepts either a commit hash or a vLLM release tag.\n- A new `publish_nightly_main` checkbox is available for daily\ncommit-mode builds.\n- Release commit-mode builds publish `nightly-main[-suffix]` images\ndirectly.\n- Daily commit-mode builds can explicitly promote their output to the\nsame release-compatible image names.\n\nThe publishing option defaults to `false`, so daily commit builds retain\ntheir temporary-only behavior unless promotion is explicitly requested.\n\nScheduled builds, pull request label builds, and existing daily builds\nthat do not enable this option retain their previous publishing\nbehavior.\n\n### How was this patch tested?\n\n\n- vLLM main:\nhttps://github.com/vllm-project/vllm/commit/ced6857afa0ea7b2e3f0846a62e1394e90f15607\n\n---------\n\nSigned-off-by: Mrz20 <2609716663@qq.com>",
      "parents": [
        "ba945e40f81894930b6884ce608cd4c1211a0f28"
      ],
      "stats": {
        "total_additions": 86,
        "total_deletions": 56,
        "files_changed": 4
      },
      "files": [
        {
          "filename": ".github/workflows/_schedule_image_build.yaml",
          "status": "modified",
          "additions": 34,
          "deletions": 19,
          "patch": "@@ -50,7 +50,7 @@ on:\n         type: string\n         default: 'digests'\n       vllm_commit:\n-        description: 'vLLM commit hash to checkout (used when set; mutually exclusive with tag/branch).'\n+        description: 'vLLM commit hash or release tag to checkout (mutually exclusive with tag/branch).'\n         required: false\n         type: string\n         default: ''\n@@ -64,6 +64,11 @@ on:\n         required: false\n         type: boolean\n         default: false\n+      publish_nightly_main:\n+        description: 'If true, daily builds also publish release-compatible nightly-main tags.'\n+        required: false\n+        type: boolean\n+        default: false\n       build_type:\n         description: 'Build type (release/daily)'\n         required: false\n@@ -206,12 +211,14 @@ jobs:\n             echo \"Error: 'vllm_commit' and 'vllm_ascend_commit' must be specified together.\"\n             exit 1\n           fi\n-          for c in \"$vllm_commit\" \"$vllm_ascend_commit\"; do\n-            if ! echo \"$c\" | grep -Eq '^[0-9a-f]{7,40}$'; then\n-              echo \"Error: invalid commit format: $c (expected 7-40 hex chars).\"\n-              exit 1\n-            fi\n-          done\n+          if ! echo \"$vllm_commit\" | grep -Eq '^[0-9a-f]{7,40}$|^v[0-9]+(\\.[0-9]+){2}((a|b|rc)[0-9]+|(\\.post|\\.dev)[0-9]+)?$'; then\n+            echo \"Error: invalid vLLM commit or release tag: $vllm_commit.\"\n+            exit 1\n+          fi\n+          if ! echo \"$vllm_ascend_commit\" | grep -Eq '^[0-9a-f]{7,40}$'; then\n+            echo \"Error: invalid vllm-ascend commit format: $vllm_ascend_commit (expected 7-40 hex chars).\"\n+            exit 1\n+          fi\n           commit_mode=\"true\"\n         fi\n \n@@ -371,7 +378,8 @@ jobs:\n           ASCEND_INDEX_URL=http://cache-service.nginx-pypi-cache.svc.cluster.local/ascend/repos/pypi\n           GIT_PROXY=https://gh-proxy.test.osinfra.cn/\n           COMPILE_CUSTOM_KERNELS=${{ steps.cache-csrc.outputs.cache-hit == 'true' && '0' || '1' }}\n-          VLLM_COMMIT=${{ inputs.vllm_commit }}\n+          ${{ inputs.vllm_commit != '' && !startsWith(inputs.vllm_commit, 'v') && format('VLLM_COMMIT={0}', inputs.vllm_commit) || '' }}\n+          ${{ startsWith(inputs.vllm_commit, 'v') && format('VLLM_TAG={0}', inputs.vllm_commit) || '' }}\n           BUILD_TYPE=${{ inputs.build_type || 'release' }}\n           MEMCACHE_VERSION=${{ inputs.memcache_version || '1.3.0' }}\n           MEMCACHE_DATE=${{ inputs.memcache_date || '20260921.3' }}\n@@ -399,7 +407,10 @@ jobs:\n           SUFFIX=\"-${{ inputs.suffix }}\"\n         fi\n         if [ \"${{ inputs.temp_only }}\" = \"true\" ]; then\n-          VLLM_SHORT=$(echo \"${{ inputs.vllm_commit }}\" | cut -c1-7)\n+          case \"${{ inputs.vllm_commit }}\" in\n+            v*) VLLM_SHORT=\"${{ inputs.vllm_commit }}\" ;;\n+            *) VLLM_SHORT=$(echo \"${{ inputs.vllm_commit }}\" | cut -c1-7) ;;\n+          esac\n           ASCEND_SHORT=$(echo \"${{ inputs.vllm_ascend_commit }}\" | cut -c1-7)\n           TAG=\"vllm-ascend-${VLLM_SHORT}-${ASCEND_SHORT}${SUFFIX}-${{ matrix.runner_info.tag }}-temp\"\n         else\n@@ -529,17 +540,15 @@ jobs:\n           #    - inputs.tag=main       --> vllm-ascend:main[-suffix]\n           #    - inputs.tag=v0.16.0rc1 --> vllm-ascend:v0.16.0rc1[-suffix]\n           tags: |\n-            # In daily mode, only the CANN-versioned daily tag is published; the pep440,\n-            # schedule and workflow_dispatch_tag rules would emit bare/plain tags\n-            # (e.g. main-310p, nightly-main-310p) without date info, so they are disabled\n-            # for daily builds only.\n+            # Daily builds normally publish only the CANN-versioned tag. Commit-mode\n+            # promotion can instead publish the release-compatible nightly-main tag.\n             type=pep440,pattern={{raw}},suffix=${{ env.SUFFIX }},enable=${{ inputs.build_type != 'daily' }}\n             type=schedule,pattern=${{ steps.tag_pattern.outputs.pattern }},prefix=nightly-,suffix=${{ env.SUFFIX }},enable=${{ inputs.build_type != 'daily' }}\n             type=raw,value=${{ inputs.workflow_dispatch_tag }},enable=${{ github.event_name == 'workflow_dispatch' && inputs.workflow_dispatch_tag != '' && inputs.build_type != 'daily' }},suffix=${{ env.SUFFIX }}\n             type=raw,value=${{ steps.tag_pattern.outputs.pattern }},enable=${{ github.event_name == 'workflow_dispatch' && inputs.workflow_dispatch_tag == '' }},prefix=nightly-,suffix=${{ env.SUFFIX }}\n             \n             # ===== New: Daily scenario specific tags =====\n-            type=raw,value=${{ steps.daily_tags.outputs.daily_tag }},enable=${{ inputs.build_type == 'daily' }}\n+            type=raw,value=${{ steps.daily_tags.outputs.daily_tag }},enable=${{ inputs.build_type == 'daily' && !inputs.publish_nightly_main }}\n             # =============================================\n           flavor:\n             latest=false\n@@ -572,12 +581,12 @@ jobs:\n           echo \"Current tags:\"\n           echo \"$TAGS\"\n \n-          # For daily builds, skip the plain schedule tag (e.g. nightly-main-310p-openeuler)\n-          # because it's redundant with the daily tag that includes CANN version + date\n+          # Daily builds skip the plain tag unless explicitly promoted for the\n+          # release-compatible nightly-ci pipeline.\n           UNWANTED_TAG=\"nightly-${SCHEDULE_TAG_PATTERN}${SUFFIX}\"\n \n           for tag in $TAGS; do\n-            if [ \"$BUILD_TYPE\" = \"daily\" ] && [ \"$tag\" = \"${{ env.SWR_REPO }}:${UNWANTED_TAG}\" ]; then\n+            if [ \"$BUILD_TYPE\" = \"daily\" ] && [ \"${{ inputs.publish_nightly_main }}\" != \"true\" ] && [ \"$tag\" = \"${{ env.SWR_REPO }}:${UNWANTED_TAG}\" ]; then\n               echo \"Skipping unwanted schedule tag: $tag\"\n               continue\n             fi\n@@ -690,7 +699,10 @@ jobs:\n           if [ -n \"${{ inputs.suffix }}\" ]; then\n             SUFFIX=\"-${{ inputs.suffix }}\"\n           fi\n-          VLLM_SHORT=$(echo \"${{ inputs.vllm_commit }}\" | cut -c1-7)\n+          case \"${{ inputs.vllm_commit }}\" in\n+            v*) VLLM_SHORT=\"${{ inputs.vllm_commit }}\" ;;\n+            *) VLLM_SHORT=$(echo \"${{ inputs.vllm_commit }}\" | cut -c1-7) ;;\n+          esac\n           ASCEND_SHORT=$(echo \"${{ inputs.vllm_ascend_commit }}\" | cut -c1-7)\n           TAG=\"${IMAGE}:vllm-ascend-${VLLM_SHORT}-${ASCEND_SHORT}${SUFFIX}\"\n \n@@ -739,7 +751,10 @@ jobs:\n           if [ -n \"${{ inputs.suffix }}\" ]; then\n             SUFFIX=\"-${{ inputs.suffix }}\"\n           fi\n-          VLLM_SHORT=$(echo \"${{ inputs.vllm_commit }}\" | cut -c1-7)\n+          case \"${{ inputs.vllm_commit }}\" in\n+            v*) VLLM_SHORT=\"${{ inputs.vllm_commit }}\" ;;\n+            *) VLLM_SHORT=$(echo \"${{ inputs.vllm_commit }}\" | cut -c1-7) ;;\n+          esac\n           ASCEND_SHORT=$(echo \"${{ inputs.vllm_ascend_commit }}\" | cut -c1-7)\n           # Temp tags were created in SWR by build-push-digest, so clean them up there.\n           # Skip cleanup if skopeo is not available (no sudo/root on this runner)."
        },
        {
          "filename": ".github/workflows/schedule_image_build_and_push.yaml",
          "status": "modified",
          "additions": 34,
          "deletions": 35,
          "patch": "@@ -25,47 +25,46 @@ on:\n     types: [ labeled ]\n   workflow_dispatch:\n     inputs:\n-      tag:\n-        description: 'Docker tag for build results (mutually exclusive with branch)'\n+      docker_ref:\n+        description: 'Docker source: main/releases/* are branches; v* and tag:main are tags; use none for commit mode.'\n         default: \"none\"\n         required: false\n         type: choice\n         options:\n-          - main\n-          - v0.26.0rc1\n-          - v0.23.0\n-          - v0.23.0rc1\n-          - v0.22.1rc1\n-          - v0.21.0rc1\n-          - v0.20.2rc1\n-          - none\n-      branch:\n-        description: 'Docker branch for build results (mutually exclusive with tag)'\n-        default: \"none\"\n-        required: false\n-        type: choice\n-        options:\n-          - main\n-          - releases/v0.27.1rc\n-          - releases/v0.26.0rc\n-          - releases/v0.25.1rc\n-          - releases/v0.24.0rc\n-          - releases/v0.23.0\n-          - releases/v0.22.1rc\n-          - releases/v0.21.0rc\n-          - releases/v0.20.2rc\n-          - releases/v0.18.0\n-          - none\n+          - 'main'\n+          - 'releases/v0.27.1rc'\n+          - 'releases/v0.26.0rc'\n+          - 'releases/v0.25.1rc'\n+          - 'releases/v0.24.0rc'\n+          - 'releases/v0.23.0'\n+          - 'releases/v0.22.1rc'\n+          - 'releases/v0.21.0rc'\n+          - 'releases/v0.20.2rc'\n+          - 'releases/v0.18.0'\n+          - 'tag:main'\n+          - 'v0.27.1rc1'\n+          - 'v0.26.0rc1'\n+          - 'v0.23.0'\n+          - 'v0.23.0rc1'\n+          - 'v0.22.1rc1'\n+          - 'v0.21.0rc1'\n+          - 'v0.20.2rc1'\n+          - 'none'\n       vllm_commit:\n-        description: 'vLLM commit hash (must be paired with vllm_ascend_commit; mutually exclusive with tag/branch). Image will be pushed only to SWR temp repo.'\n+        description: 'vLLM commit hash or release tag, e.g. v0.29.0. Pair with vllm_ascend_commit and set docker_ref to none.'\n         required: false\n         default: ''\n         type: string\n       vllm_ascend_commit:\n-        description: 'vllm-ascend commit hash (must be paired with vllm_commit; mutually exclusive with tag/branch). Image will be pushed only to SWR temp repo.'\n+        description: 'vllm-ascend commit hash. Pair with vllm_commit and set docker_ref to none.'\n         required: false\n         default: ''\n         type: string\n+      publish_nightly_main:\n+        description: 'Daily commit mode only: also publish release-compatible nightly-main tags.'\n+        required: false\n+        default: false\n+        type: boolean\n       build_type:\n         description: 'Build type (release/daily)'\n         required: false\n@@ -157,8 +156,8 @@ jobs:\n       # ======================================================================\n       \n       should_push: ${{ github.repository == 'vllm-project/vllm-ascend' && github.event_name != 'pull_request' }}\n-      workflow_dispatch_tag: ${{ inputs.tag != 'none' && inputs.tag || '' }}\n-      branch_ref: ${{ inputs.branch != 'none' && inputs.branch || '' }}\n+      workflow_dispatch_tag: ${{ inputs.docker_ref == 'tag:main' && 'main' || (startsWith(inputs.docker_ref, 'v') && inputs.docker_ref || '') }}\n+      branch_ref: ${{ (inputs.docker_ref == 'main' || startsWith(inputs.docker_ref, 'releases/')) && inputs.docker_ref || '' }}\n       schedule_tag_pattern: 'main'\n       build_type: ${{ needs.setup-matrix.outputs.build_type }}\n       cann_version: ${{ fromJSON(inputs.cann || '[\"swr.cn-north-12.myhuaweicloud.com/atlas_inference/cann\", \"9.1.0_20260731131545\"]')[1] }}\n@@ -185,8 +184,7 @@ jobs:\n     if: ${{ github.event_name == 'workflow_dispatch'\n         && inputs.vllm_commit != ''\n         && inputs.vllm_ascend_commit != ''\n-        && inputs.tag == 'none'\n-        && inputs.branch == 'none' }}\n+        && inputs.docker_ref == 'none' }}\n     strategy:\n       fail-fast: false\n       max-parallel: 8\n@@ -203,7 +201,8 @@ jobs:\n       swr_registry: 'swr.cn-north-12.myhuaweicloud.com'\n       swr_temp_repo: ${{ needs.setup-matrix.outputs.build_type == 'daily' && 'swr.cn-north-12.myhuaweicloud.com/atlas_inference/vllm-atlas-temp' || 'swr.cn-north-12.myhuaweicloud.com/atlas_ci/vllm-atlas-temp' }}\n       should_push: true\n-      temp_only: true\n+      temp_only: ${{ needs.setup-matrix.outputs.build_type == 'daily' && !inputs.publish_nightly_main }}\n+      publish_nightly_main: ${{ inputs.publish_nightly_main }}\n       vllm_commit: ${{ inputs.vllm_commit }}\n       vllm_ascend_commit: ${{ inputs.vllm_ascend_commit }}\n       workflow_dispatch_tag: ''\n@@ -225,4 +224,4 @@ jobs:\n     secrets:\n       SWR_USERNAME: ${{ secrets.SWR_USERNAME }}\n       SWR_PASSWORD: ${{ secrets.SWR_PASSWORD }}\n-      SYNC_TOOLS_TOKEN: ${{ secrets.SYNC_TOOLS_TOKEN }}\n\\ No newline at end of file\n+      SYNC_TOOLS_TOKEN: ${{ secrets.SYNC_TOOLS_TOKEN }}"
        },
        {
          "filename": "Dockerfile.a5",
          "status": "modified",
          "additions": 9,
          "deletions": 1,
          "patch": "@@ -45,6 +45,8 @@ ARG MOONCAKE_TAG=0.3.11.post1\n ARG VLLM_REPO=https://github.com/vllm-project/vllm.git\n # VLLM_TAG (optional): vLLM release tag to clone when VLLM_COMMIT is empty.\n ARG VLLM_TAG=v0.30.0\n+# VLLM_COMMIT (optional): Exact vLLM commit to build; empty to fall back to VLLM_TAG.\n+ARG VLLM_COMMIT=\"\"\n # SOC_VERSION (optional): Ascend SoC version used for custom kernel compilation.\n ARG SOC_VERSION=\"ascend950dt_9582\"\n # COMPILE_CUSTOM_KERNELS (optional): Whether to compile custom Ascend kernels (1 = yes, 0 = no).\n@@ -107,7 +109,13 @@ RUN pip config set global.index-url ${PIP_INDEX_URL} && \\\n \n # Install vLLM\n RUN if [ -n \"$GIT_PROXY\" ]; then git config --global url.\"${GIT_PROXY}https://github.com/\".insteadOf https://github.com/; fi && \\\n-    git clone --depth 1 -b $VLLM_TAG $VLLM_REPO /vllm-workspace/vllm\n+    if [ -n \"$VLLM_COMMIT\" ]; then \\\n+      git init /vllm-workspace/vllm && \\\n+      git -C /vllm-workspace/vllm fetch --depth 1 $VLLM_REPO \"$VLLM_COMMIT\" && \\\n+      git -C /vllm-workspace/vllm checkout FETCH_HEAD; \\\n+    else \\\n+      git clone --depth 1 -b $VLLM_TAG $VLLM_REPO /vllm-workspace/vllm; \\\n+    fi\n # In x86, triton will be installed by vllm. But in Ascend, triton doesn't work correctly. we need to uninstall it.\n RUN VLLM_TARGET_DEVICE=\"empty\" python3 -m pip install -e /vllm-workspace/vllm/[audio] --extra-index-url ${PYTORCH_INDEX_URL} && \\\n     python3 -m pip uninstall -y triton && \\"
        },
        {
          "filename": "Dockerfile.a5.openEuler",
          "status": "modified",
          "additions": 9,
          "deletions": 1,
          "patch": "@@ -45,6 +45,8 @@ ARG MOONCAKE_TAG=0.3.11.post1\n ARG VLLM_REPO=https://github.com/vllm-project/vllm.git\n # VLLM_TAG (optional): vLLM release tag to clone when VLLM_COMMIT is empty.\n ARG VLLM_TAG=v0.30.0\n+# VLLM_COMMIT (optional): Exact vLLM commit to build; empty to fall back to VLLM_TAG.\n+ARG VLLM_COMMIT=\"\"\n # SOC_VERSION (optional): Ascend SoC version used for custom kernel compilation.\n ARG SOC_VERSION=\"ascend950dt_9582\"\n # COMPILE_CUSTOM_KERNELS (optional): Whether to compile custom Ascend kernels (1 = yes, 0 = no).\n@@ -104,7 +106,13 @@ RUN pip config set global.index-url ${PIP_INDEX_URL} && \\\n \n # Install vLLM\n RUN if [ -n \"$GIT_PROXY\" ]; then git config --global url.\"${GIT_PROXY}https://github.com/\".insteadOf https://github.com/; fi && \\\n-    git clone --depth 1 -b $VLLM_TAG $VLLM_REPO /vllm-workspace/vllm\n+    if [ -n \"$VLLM_COMMIT\" ]; then \\\n+      git init /vllm-workspace/vllm && \\\n+      git -C /vllm-workspace/vllm fetch --depth 1 $VLLM_REPO \"$VLLM_COMMIT\" && \\\n+      git -C /vllm-workspace/vllm checkout FETCH_HEAD; \\\n+    else \\\n+      git clone --depth 1 -b $VLLM_TAG $VLLM_REPO /vllm-workspace/vllm; \\\n+    fi\n # In x86, triton will be installed by vllm. But in Ascend, triton doesn't work correctly. we need to uninstall it.\n RUN VLLM_TARGET_DEVICE=\"empty\" python3 -m pip install -e /vllm-workspace/vllm/[audio] --extra-index-url ${PYTORCH_INDEX_URL} && \\\n     python3 -m pip uninstall -y triton && \\\n"
        }
      ]
    },
    {
      "sha": "ba945e40f81894930b6884ce608cd4c1211a0f28",
      "author": {
        "name": "LQDLove",
        "email": "34787704+LQDLove@users.noreply.github.com"
      },
      "date": "2026-09-25T15:12:27+08:00",
      "message": "Revert \"[BugFix] Solve the d2h free_bubble in glm5.2 dspark\" (#17184) (#17525)\n\n### What this PR does / why we need it?\n\nReverts #17184 (\"[BugFix] Solve the d2h free_bubble in glm5.2 dspark\").\n\nThe seq_lens source selection introduced by #17184 needs to be rolled\nback from main. Reverting restores the original behavior in\n`AscendAttentionBackend.build`:\n\n- `_select_seq_lens` helper removed from\n`vllm_ascend/attention/utils.py`\n- `vllm_ascend/attention/attention_v1.py` back to inline CPU-mirror\npreference (`_seq_lens_cpu` -> `seq_lens_cpu` -> device `.to(\"cpu\")`),\nwith cross-attention / parallel-drafting overrides inline\n- `_is_glm_model` moved back to\n`vllm_ascend/spec_decode/llm_base_proposer.py` (its original location\nbefore #17184)\n- DSpark whitelist UTs removed from\n`tests/ut/attention/test_attention_utils.py`\n\nThe revert is clean against current main: the only overlap is\n`vllm_ascend/utils.py`, where the later sleep-related additions from\n#15340 are preserved.\n\n### Does this PR introduce _any_ user-facing change?\n\nNo.\n\n### How was this patch tested?\n\nCI verification: revert applies cleanly on top of current main; logic\nrestored is identical to the pre-#17184 state.\n\n- vLLM main:\nhttps://github.com/vllm-project/vllm/commit/ced6857afa0ea7b2e3f0846a62e1394e90f15607\n\nSigned-off-by: liaoqidan <1107297340@qq.com>",
      "parents": [
        "bea70ab8fa233a06a7483337f42a1e3f2c8a117c"
      ],
      "stats": {
        "total_additions": 26,
        "total_deletions": 150,
        "files_changed": 5
      },
      "files": [
        {
          "filename": "tests/ut/attention/test_attention_utils.py",
          "status": "modified",
          "additions": 1,
          "deletions": 90,
          "patch": "@@ -14,99 +14,10 @@\n # limitations under the License.\n \n from types import SimpleNamespace\n-from unittest.mock import MagicMock\n \n-import pytest\n import torch\n-from vllm.v1.kv_cache_interface import CrossAttentionSpec\n \n-from vllm_ascend.attention.utils import (\n-    AscendCommonAttentionMetadata,\n-    _select_seq_lens,\n-    filter_chunked_req_indices,\n-    get_or_register_attention_buffer,\n-)\n-\n-NUM_REQS = 2\n-# Distinct values so the selected source (CPU mirror vs NPU tensor) is identifiable.\n-CPU_SEQ_LENS = torch.tensor([10, 20, 30], dtype=torch.int32)\n-NPU_SEQ_LENS = torch.tensor([99, 99, 99], dtype=torch.int32)\n-\n-\n-def _common_attn_metadata() -> AscendCommonAttentionMetadata:\n-    return AscendCommonAttentionMetadata(\n-        query_start_loc=torch.tensor([0, 2, 5]),\n-        query_start_loc_cpu=torch.tensor([0, 2, 5]),\n-        seq_lens=NPU_SEQ_LENS,\n-        _seq_lens_cpu=CPU_SEQ_LENS,\n-        num_reqs=NUM_REQS,\n-        num_actual_tokens=5,\n-        max_query_len=3,\n-        max_seq_len=30,\n-        block_table_tensor=torch.zeros((NUM_REQS, 4), dtype=torch.int32),\n-        slot_mapping=torch.arange(5, dtype=torch.int32),\n-    )\n-\n-\n-def _spec_config(method: str, parallel_drafting: bool) -> MagicMock:\n-    spec_config = MagicMock()\n-    spec_config.parallel_drafting = parallel_drafting\n-    spec_config.use_dspark.return_value = method == \"dspark\"\n-    return spec_config\n-\n-\n-def _vllm_config(model_type: str) -> SimpleNamespace:\n-    return SimpleNamespace(\n-        model_config=SimpleNamespace(hf_text_config=SimpleNamespace(model_type=model_type)),\n-    )\n-\n-\n-def test_select_seq_lens_defaults_to_cpu_mirror() -> None:\n-    seq_lens = _select_seq_lens(_common_attn_metadata(), None, None, _vllm_config(\"glm5_next\"))\n-\n-    torch.testing.assert_close(seq_lens, CPU_SEQ_LENS[:NUM_REQS])\n-\n-\n-@pytest.mark.parametrize(\n-    (\"method\", \"parallel_drafting\", \"model_type\", \"expected\"),\n-    [\n-        # DSpark on the GLM5.2 family keeps the CPU seq_lens mirror.\n-        (\"dspark\", True, \"glm5_next\", CPU_SEQ_LENS[:NUM_REQS]),\n-        (\"dspark\", True, \"glm_moe_dsa\", CPU_SEQ_LENS[:NUM_REQS]),\n-        # DSpark on other models and other parallel-drafting methods (DFlash,\n-        # PARD draft_model) keep the NPU seq_lens.\n-        (\"dspark\", True, \"qwen3\", NPU_SEQ_LENS),\n-        (\"dflash\", True, \"glm5_next\", NPU_SEQ_LENS),\n-        (\"draft_model\", True, \"glm5_next\", NPU_SEQ_LENS),\n-        # Non-parallel spec decode keeps the CPU mirror.\n-        (\"mtp\", False, \"glm5_next\", CPU_SEQ_LENS[:NUM_REQS]),\n-    ],\n-)\n-def test_select_seq_lens_spec_decode(\n-    method: str,\n-    parallel_drafting: bool,\n-    model_type: str,\n-    expected: torch.Tensor,\n-) -> None:\n-    seq_lens = _select_seq_lens(\n-        _common_attn_metadata(),\n-        None,\n-        _spec_config(method, parallel_drafting),\n-        _vllm_config(model_type),\n-    )\n-\n-    torch.testing.assert_close(seq_lens, expected)\n-\n-\n-def test_select_seq_lens_cross_attention_uses_npu_seq_lens() -> None:\n-    kv_cache_spec = CrossAttentionSpec(block_size=16, num_kv_heads=8, head_size=128, dtype=torch.float16)\n-\n-    # Cross-attention wins even under dspark + GLM5.2.\n-    seq_lens = _select_seq_lens(\n-        _common_attn_metadata(), kv_cache_spec, _spec_config(\"dspark\", True), _vllm_config(\"glm5_next\")\n-    )\n-\n-    assert seq_lens is NPU_SEQ_LENS\n+from vllm_ascend.attention.utils import filter_chunked_req_indices, get_or_register_attention_buffer\n \n \n def test_get_or_register_attention_buffer() -> None:"
        },
        {
          "filename": "vllm_ascend/attention/attention_v1.py",
          "status": "modified",
          "additions": 11,
          "deletions": 8,
          "patch": "@@ -46,7 +46,6 @@ from vllm_ascend.ascend_forward_context import _EXTRA_CTX\n from vllm_ascend.attention.attention_mask import AttentionMaskBuilder\n from vllm_ascend.attention.utils import (\n     AscendCommonAttentionMetadata,\n-    _select_seq_lens,\n     enable_dcp,\n     needs_layer_aware_fia_graph_replay,\n     notify_kv_cache_written,\n@@ -314,19 +313,23 @@ class AscendAttentionMetadataBuilder(AttentionMetadataBuilder[AscendMetadata]):\n         )\n \n         block_table = common_attn_metadata.block_table_tensor\n+        # Prefer _seq_lens_cpu (always available, updated during draft\n+        # iterations) over seq_lens_cpu (None in async spec decode mode).\n+        if common_attn_metadata._seq_lens_cpu is not None:\n+            seq_lens = common_attn_metadata._seq_lens_cpu[:num_reqs]\n+        elif common_attn_metadata.seq_lens_cpu is not None:\n+            seq_lens = common_attn_metadata.seq_lens_cpu[:num_reqs]\n+        else:\n+            seq_lens = common_attn_metadata.seq_lens[:num_reqs].to(\"cpu\")\n \n-        # TODO: This will be moved to propose and deleted after fia ops fixed.\n-        seq_lens = _select_seq_lens(\n-            common_attn_metadata,\n-            kv_cache_spec=self.kv_cache_spec,\n-            speculative_config=self.speculative_config,\n-            vllm_config=self.vllm_config,\n-        )\n         slot_mapping = common_attn_metadata.slot_mapping[:num_actual_tokens]\n         # this slot_mapping override doesn't work since vllm will override it again. We should fix it vllm.\n         # see: https://github.com/vllm-project/vllm/blob/ce88756b967c2c5006746a424c15dd59a284ed8c/vllm/model_executor/layers/attention/cross_attention.py#L117\n         if isinstance(self.kv_cache_spec, CrossAttentionSpec):\n+            seq_lens = common_attn_metadata.seq_lens\n             slot_mapping = common_attn_metadata.slot_mapping.to(torch.int32)\n+        elif self.speculative_config and self.speculative_config.parallel_drafting:\n+            seq_lens = common_attn_metadata.seq_lens\n \n         attn_state = common_attn_metadata.attn_state\n "
        },
        {
          "filename": "vllm_ascend/attention/utils.py",
          "status": "modified",
          "additions": 0,
          "deletions": 40,
          "patch": "@@ -7,18 +7,15 @@ from typing import Any\n import torch\n import torch.nn.functional as F\n from vllm.config import VllmConfig, get_current_vllm_config\n-from vllm.config.speculative import SpeculativeConfig\n from vllm.distributed.kv_transfer import get_kv_transfer_group, has_kv_transfer_group, is_v1_kv_transfer_group\n from vllm.forward_context import ForwardContext, get_forward_context\n from vllm.utils.torch_utils import get_dtype_size\n from vllm.v1.attention.backend import MLAAttentionImpl\n from vllm.v1.attention.backends.utils import CommonAttentionMetadata\n-from vllm.v1.kv_cache_interface import AttentionSpec, CrossAttentionSpec\n \n from vllm_ascend.device.hardware_profile import HardwareCapability, get_current_hardware_profile\n from vllm_ascend.device.utils import FIA_TND_LARGE_HEAD_FALLBACK_HEAD_SIZE\n from vllm_ascend.utils import (\n-    _is_glm_model,\n     get_ascend_config,\n     is_pd_decode_recompute_scheduler_enabled,\n )\n@@ -548,40 +545,3 @@ def enabling_mlapo(vllm_config: VllmConfig) -> bool:\n         and not vllm_config.kv_transfer_config.is_kv_producer\n     )\n     return bool(config_val and is_decode_instance)\n-\n-\n-def _select_seq_lens(\n-    common_attn_metadata: AscendCommonAttentionMetadata,\n-    kv_cache_spec: AttentionSpec | None,\n-    speculative_config: SpeculativeConfig | None,\n-    vllm_config: VllmConfig,\n-) -> torch.Tensor:\n-    \"\"\"Choose the seq_lens tensor carried by the built attention metadata.\n-\n-    Defaults to the CPU mirror: ``_seq_lens_cpu`` is always available and\n-    updated during draft iterations, while ``seq_lens_cpu`` is None in async\n-    spec decode mode. Cross-attention and generic parallel drafting override\n-    this with the NPU ``seq_lens``; the one exception is DSpark on the GLM5.2\n-    family, whose CPU mirror carries the same post-rejection-sampling lengths\n-    across draft iterations, so building from it skips the NPU->CPU sync at\n-    ``seq_lens.tolist()`` in the metadata build.\n-    \"\"\"\n-    # Prefer _seq_lens_cpu (always available, updated during draft\n-    # iterations) over seq_lens_cpu (None in async spec decode mode).\n-    model_config = vllm_config.model_config\n-    num_reqs = common_attn_metadata.num_reqs\n-    if common_attn_metadata._seq_lens_cpu is not None:\n-        seq_lens = common_attn_metadata._seq_lens_cpu[:num_reqs]\n-    elif common_attn_metadata.seq_lens_cpu is not None:\n-        seq_lens = common_attn_metadata.seq_lens_cpu[:num_reqs]\n-    else:\n-        seq_lens = common_attn_metadata.seq_lens[:num_reqs].to(\"cpu\")\n-\n-    if isinstance(kv_cache_spec, CrossAttentionSpec):\n-        seq_lens = common_attn_metadata.seq_lens\n-    if speculative_config is not None and speculative_config.parallel_drafting:\n-        # PARD / DFlash (and DSpark on other models) keep the NPU seq_lens;\n-        # only DSpark on the GLM5.2 family keeps the CPU mirror above.\n-        if not (speculative_config.use_dspark() and _is_glm_model(model_config)):\n-            seq_lens = common_attn_metadata.seq_lens\n-    return seq_lens"
        },
        {
          "filename": "vllm_ascend/spec_decode/llm_base_proposer.py",
          "status": "modified",
          "additions": 14,
          "deletions": 1,
          "patch": "@@ -64,7 +64,7 @@ from vllm_ascend.spec_decode.utils import (\n     _maybe_eager_context,\n     patch_tensor_parallel_group,\n )\n-from vllm_ascend.utils import _is_glm_model, check_gdn_layer, enable_sp, lmhead_tp_enable, use_updatable_graph\n+from vllm_ascend.utils import check_gdn_layer, enable_sp, lmhead_tp_enable, use_updatable_graph\n from vllm_ascend.worker.device_metadata import DeviceMetadataTask, DeviceMetadataTaskProvider\n \n \n@@ -110,6 +110,19 @@ def greedy_sample(logits: torch.Tensor) -> torch.Tensor:\n     return target_argmax\n \n \n+# TODO(lilinsiman): Remove this code segment after future versions of the GLM\n+# series models support graph input for speculative inference.\n+def _is_glm_model(model_config) -> bool:\n+    \"\"\"Return True if the target model belongs to the GLM series.\n+\n+    Detection is based on the model_type string (covers glm, chatglm, glm4,\n+    glm4_moe, glm4_moe_lite, glm4_1v, glm_ocr, glm_moe_dsa, etc).\n+    \"\"\"\n+    hf_text_config = getattr(model_config, \"hf_text_config\", None)\n+    model_type = getattr(hf_text_config, \"model_type\", \"\") or \"\"\n+    return \"glm\" in str(model_type).lower()\n+\n+\n class AscendSpecDecodeBaseProposer(SpecDecodeBaseProposer):\n     _runnable: ACLGraphWrapper | Callable\n     arange: torch.Tensor"
        },
        {
          "filename": "vllm_ascend/utils.py",
          "status": "modified",
          "additions": 0,
          "deletions": 11,
          "patch": "@@ -1776,14 +1776,3 @@ def use_updatable_graph(\n     from vllm_ascend.attention.attention_v1 import AscendAttentionBackend\n \n     return attn_backend is not None and issubclass(attn_backend, AscendAttentionBackend)\n-\n-\n-def _is_glm_model(model_config) -> bool:\n-    \"\"\"Return True if the target model belongs to the GLM series.\n-\n-    Detection is based on the model_type string (covers glm, chatglm, glm4,\n-    glm4_moe, glm4_moe_lite, glm4_1v, glm_ocr, glm_moe_dsa, etc).\n-    \"\"\"\n-    hf_text_config = getattr(model_config, \"hf_text_config\", None)\n-    model_type = getattr(hf_text_config, \"model_type\", \"\") or \"\"\n-    return \"glm\" in str(model_type).lower()\n"
        }
      ]
    }
  ]
}