{
  "date": "2026-08-09",
  "lessons": [
    {
      "id": "L20260809-001",
      "title": "Triton kernel: dynamic-bound loops must not use Python range()",
      "keywords": [
        "Triton range dynamic bounds",
        "postprocess_mamba_fused_kernel",
        "compile-time constant"
      ],
      "symptom": "Review flagged vllm_ascend/ops/triton/mamba/postprocess.py: `for i in range(0, copy_size, COPY_BLOCK_SIZE)` where copy_size is a runtime value — Triton evaluates range() at compile time and requires constexpr bounds, so compilation fails (or silently miscompiles) once the copy path runs.",
      "root_cause": "The Triton kernel copy loop was written with Python range() semantics; Triton only allows compile-time-constant bounds there.  Runtime bounds need a `while` loop (Triton 2.x+) or tl.static_range with a constexpr cap + runtime mask.",
      "fix_guidance": [
        "In any Triton kernel, check every loop bound: if it is loaded from a pointer, derived from runtime tensors, or passed as a non-constexpr arg, it is NOT valid for range()",
        "Use `while i < copy_size:` with `i += BLOCK` or `tl.static_range(MAX_ITERS)` + `if i < num_iters:` mask",
        "After syncing a kernel from upstream, grep for `range(` with non-constexpr bounds before merging"
      ],
      "tags": [
        "triton",
        "kernel",
        "mamba",
        "review",
        "compile"
      ],
      "example": "gemini code review on PR #13823 flagged mamba/postprocess.py:166; the upstream vllm kernel this was synced from uses the same loop shape, so verify against upstream's while-loop pattern.",
      "created_at": "2026-08-09T11:27:11.462272+08:00",
      "hits": 0
    },
    {
      "id": "L20260809-002",
      "title": "E2E model/LoRA adapter download failures are environment, not adaptation",
      "keywords": [
        "No adapter found",
        "add_lora failed",
        "model cache"
      ],
      "symptom": "test_qwen3_multi_loras failed all 3 e2e rounds with `Exception: Call to add_lora method failed: Loading lora Alice failed: No adapter found for charent/self_cognition_Alice` — the LoRA adapter model is missing from the runner cache and cannot be downloaded.",
      "root_cause": "The e2e case needs a model/LoRA adapter that is not pre-cached on the runner; download fails (offline/cache miss).  This is an environment gap, not an adaptation defect — 3 fix rounds were wasted on it and the whole run was discarded.",
      "fix_guidance": [
        "Before treating an e2e failure as an adaptation bug, check whether the error is a model download / adapter-not-found / HF offline message — that is an env issue, skip the case or pre-cache the model",
        "A single environment-dependent case failing 3 rounds discards the whole run (no commit, no PR) — classify env failures early"
      ],
      "tags": [
        "e2e",
        "environment",
        "lora",
        "model-download"
      ],
      "example": "run 31262305861: multi_loras failed rounds 0-2 with adapter-not-found; npu_ipc_weight_transfer failed alongside.",
      "created_at": "2026-08-09T11:27:12.636191+08:00",
      "hits": 0
    },
    {
      "id": "L20260809-003",
      "title": "npu_ipc_weight_transfer e2e fails persistently (needs investigation)",
      "keywords": [
        "npu_ipc_weight_transfer",
        "server weights",
        "weight transfer e2e"
      ],
      "symptom": "test_npu_ipc_weight_transfer_updates_server_weights failed all 3 e2e rounds of run 31262305861 even after the adapter fixed npu_ipc_engine.py and the test file — the server-side weight update path raises at runtime.",
      "root_cause": "Not yet diagnosed: either a real adaptation gap in the trainer weight-transfer runtime path or an environment issue (server startup/connection).  The failure is persistent across rounds, so it is likely real — needs a targeted traceback review before the case can be trusted as a per-step signal.",
      "fix_guidance": [
        "When a case fails 3 consecutive rounds with the same test, read the round-N traceback directly (it is in the workspace artifact) rather than re-adapting blind",
        "Persistent failures that are not model-download env issues are the highest-value fix targets"
      ],
      "tags": [
        "e2e",
        "weight-transfer",
        "npu-ipc",
        "persistent"
      ],
      "example": "run 31262305861 rounds 0-2; the case was removed from the per-step allowlist (test_policy.json) as a stopgap.",
      "created_at": "2026-08-09T11:27:13.602673+08:00",
      "hits": 0
    }
  ]
}