{
  "date": "2026-08-13",
  "lessons": [
    {
      "id": "L20260813-001",
      "title": "UT adaptation: bare-object tests, mock-contract drift, version-guarded symbol resolution",
      "keywords": [
        "object has no attribute",
        "AscendMoERunner",
        "ascend_expert_map",
        "all_stop_token_ids",
        "query_start_loc",
        "unexpected keyword argument",
        "test_forward_impl_returns_current_runner_contract",
        "test_ascend_expert_map_follows_model_runner",
        "test_prepare_inputs_propagates_padded_request_count",
        "test_manager_rejects_mixed_structured_output_backends",
        "bare-object",
        "version-guarded"
      ],
      "symptom": "PR CI run 31581543851 (PR #14107) cpu-0: 8 failed UT — fused_moe contract tests x4 (AttributeError: 'AscendMoERunner'/'AscendMoERunner310' object has no attribute 'gate'), test_ascend_expert_map_follows_model_runner ('AscendRoutedExperts' no 'ascend_expert_map'), test_patch_structured_output x2 (mock result wrapping AttributeError in a Future), test_prepare_inputs_propagates_padded_request_count (KeyError 'query_start_loc'). All are UTs rewritten by the main2main adapter alongside source adaptation.",
      "root_cause": "Adapting an upstream contract change, the rewritten UTs fall into three systematic gaps: (1) bare-object tests construct the object with __new__ (skipping __init__) and omit attributes the tested code path reads; (2) mocks/fakes drift from the upstream contract when the upstream adds/changes parameters, attributes, or signatures; (3) version-guarded names (vllm_version_is) have multiple definitions and lookup helpers (e.g. next() over an AST walk) grab the wrong branch — usually the stub.",
      "fix_guidance": [
        "Bare-object tests — signal: AttributeError '<X> object has no attribute '<Y>' where X is a __new__-constructed object. Fix: add the missing attribute to the test object; for optional attributes prefer defensive access in the source (getattr(obj, name, default)) — it stays compatible with upstream, which always sets them in __init__.",
        "Mock-contract drift — signal: TypeError 'unexpected keyword argument' or a mock result wrapping an AttributeError (e.g. a Future). Fix: sync the mock with the upstream definition by grepping the upstream signature/attribute; do NOT run the test to discover it.",
        "Version-guarded symbol resolution — signal: a stub/empty implementation surfaces as KeyError/AttributeError at the first real use. Fix: grep ALL 'def <name>(' and resolve the actual implementation (often the private method the stub delegates to), not the first match.",
        "Adapt source and tests together: when the adaptation changes a contract, update the test mocks in the same pass. Verify statically (grep the attribute-access chain of the bare object) instead of running tests — running is banned for the adapter and each run costs a full e2e round."
      ],
      "tags": [
        "e2e-fix",
        "ut-test-adaptation",
        "auto-submitted"
      ],
      "example": "run https://github.com/vllm-project/vllm-ascend/actions/runs/31581543851 cpu-0, PR #14107: fused_moe x4 (rule 1), routed_experts (rule 1/2), structured_output x2 (rule 2), model_runner_v2_mamba (rule 3).",
      "created_at": "2026-08-13T00:55:08.815441+08:00",
      "hits": 0
    }
  ]
}